| ID | Description | Steps | Test Data | Pre-condition | Expected Output | Passed/Failed |
|----|-------------|-------|-----------|---------------|-----------------|---------------|
| BU-ANA-01 | Search duration | 1. Run `tests/test_analytics_route.py::TestAnalytics::test_search_duration` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/analytics/search-duration', json={'search_id': seed, 'search_duration': 1, 'product_load_duration': 1}; POST '/api/analytics/search-duration', json={} | analytics blueprint mounted at /api/analytics with SQLite SearchTime/SearchQuery rows; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `seed` | r.status_code == 200; c.post('/api/analytics/search-duration', json={}).status_code == 400 | Passed |
| BU-ANA-02 | Search duration not found | 1. Run `tests/test_analytics_route.py::TestAnalytics::test_search_duration_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/analytics/search-duration', json={'search_id': 99999, 'search_duration': 1, 'product_load_duration': 1} | analytics blueprint mounted at /api/analytics with SQLite SearchTime/SearchQuery rows; active Flask application context with test DB tables available; module Flask test client inside app context | c.post('/api/analytics/search-duration', json={'search_id': 99999, 'search_duration': 1, 'product_load_duration': 1}).status_code == 404 | Passed |
| BU-ANA-03 | Search duration internal error | 1. Run `tests/test_analytics_route.py::TestAnalytics::test_search_duration_internal_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/analytics/search-duration', json={'search_id': 1, 'search_duration': 1, 'product_load_duration': 1} | analytics blueprint mounted at /api/analytics with SQLite SearchTime/SearchQuery rows; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.analytics.SearchService.update_client_metrics; Mock/setup values: mu.side_effect = Exception('err') | r.status_code == 500 | Passed |
| BU-ANA-04 | Logs exception | 1. Run `tests/test_analytics_route.py::TestAnalytics::test_logs_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/analytics/logs'; GET '/api/analytics/logs/1' | analytics blueprint mounted at /api/analytics with SQLite SearchTime/SearchQuery rows; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.analytics.SearchTime; Mock/setup values: ms.query.all.side_effect = Exception('err'); ms.query.get.side_effect = Exception('err') | c.get('/api/analytics/logs').status_code == 500; c.get('/api/analytics/logs/1').status_code == 500 | Passed |
| BU-ANA-05 | Logs | 1. Run `tests/test_analytics_route.py::TestAnalytics::test_logs` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/analytics/logs'; GET f'/api/analytics/logs/{seed}'; GET '/api/analytics/logs/99999' | analytics blueprint mounted at /api/analytics with SQLite SearchTime/SearchQuery rows; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `seed` | c.get('/api/analytics/logs').status_code == 200; c.get(f'/api/analytics/logs/{seed}').status_code == 200; c.get('/api/analytics/logs/99999').status_code == 404 | Passed |
| BU-ANZ-01 | Zero duration | 1. Run `tests/test_analytics_zero.py::TestAnalyticsZeroValues::test_zero_duration` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Input variables: payload = {'search_id': self.search_id, 'search_duration': 0, 'product_load_duration': 100}; Calls: POST '/api/analytics/search-duration', data=json.dumps(payload), content_type='application/json' | unittest fixture creates app, client, SearchQuery, SearchTime row, and active app context | self.assertEqual(response.status_code, 200); self.assertEqual(st.search_duration, 0) | Passed |
| BU-APP-01 | App creates successfully | 1. Run `tests/test_app_init.py::test_app_creates_successfully` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: app is not None; isinstance(app, Flask) | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB | app is not None; isinstance(app, Flask) | Passed |
| BU-APP-02 | All 10 blueprints registered | 1. Run `tests/test_app_init.py::test_all_10_blueprints_registered` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: 'search' in app.blueprints; 'products' in app.blueprints; 'feedback' in app.blueprints; 'health' in app.blueprints | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB | 'search' in app.blueprints; 'products' in app.blueprints; 'feedback' in app.blueprints; 'health' in app.blueprints; 'brands' in app.blueprints; 'categories' in app.blueprints; 'retrieval' in app.blueprints; 'analytics' in app.blueprints; 'bulk_faiss' in app.blueprints; 'correction' in app.blueprints; 1 additional assertions | Passed |
| BU-APP-03 | Config class loaded by env | 1. Run `tests/test_app_init.py::test_config_class_loaded_by_env` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: app.config['TESTING'] is True; app.config['SQLALCHEMY_DATABASE_URI'] == 'sqlite:///:memory:' | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB | app.config['TESTING'] is True; app.config['SQLALCHEMY_DATABASE_URI'] == 'sqlite:///:memory:' | Passed |
| BU-APP-04 | Config class defaults to development | 1. Run `tests/test_app_init.py::test_config_class_defaults_to_development` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: config_class == DevelopmentConfig | application factory/config setup is exercised with TestingConfig and patched environment where needed; pytest monkeypatch fixture | config_class == DevelopmentConfig | Passed |
| BU-APP-05 | Cors applied to all routes | 1. Run `tests/test_app_init.py::test_cors_applied_to_all_routes` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: 'Access-Control-Allow-Origin' in response.headers | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask test client inside app context | 'Access-Control-Allow-Origin' in response.headers | Passed |
| BU-APP-06 | Max content length rejects oversized | 1. Run `tests/test_app_init.py::test_max_content_length_rejects_oversized` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: app.config['MAX_CONTENT_LENGTH'] == 16 * 1024 * 1024 | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB | app.config['MAX_CONTENT_LENGTH'] == 16 * 1024 * 1024 | Passed |
| BU-APP-07 | Database connection established | 1. Run `tests/test_app_init.py::test_database_connection_established` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: result.scalar() == 1 | application factory/config setup is exercised with TestingConfig and patched environment where needed; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB | result.scalar() == 1 | Passed |
| BU-BRD-01 | Get brand not found | 1. Run `tests/test_brands_route.py::TestBrands::test_get_brand_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/brands/99999' | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.get('/api/brands/99999').status_code == 404 | Passed |
| BU-BRD-02 | Sorted | 1. Run `tests/test_brands_route.py::TestBrands::test_sorted` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/brands'; Seed/input objects: Brand(name=n) | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | names == ['Apple', 'Banana', 'Zebra'] | Passed |
| BU-BRD-03 | Crud | 1. Run `tests/test_brands_route.py::TestBrands::test_crud` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/brands', json={'name': 'Nike'}; GET f'/api/brands/{bid}'; PUT f'/api/brands/{bid}', json={'name': 'Adidas'}; DELETE f'/api/brands/{bid}'; DELETE '/api/brands/99999' | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context | r.status_code == 201; c.get(f'/api/brands/{bid}').status_code == 200; c.put(f'/api/brands/{bid}', json={'name': 'Adidas'}).status_code == 200; c.delete(f'/api/brands/{bid}').status_code == 200; c.delete('/api/brands/99999').status_code == 404 | Passed |
| BU-BRD-04 | Exceptions | 1. Run `tests/test_brands_route.py::TestBrands::test_exceptions` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/brands', json={}; GET '/api/brands/99999'; PUT '/api/brands/99999', json={'name': 'X'}; DELETE '/api/brands/99999' | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.post('/api/brands', json={}).status_code == 400; c.get('/api/brands/99999').status_code == 404; c.put('/api/brands/99999', json={'name': 'X'}).status_code == 404; c.delete('/api/brands/99999').status_code == 404 | Passed |
| BU-BRD-05 | Create brand empty name | 1. Run `tests/test_brands_route.py::TestBrands::test_create_brand_empty_name` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/brands', json={'name': ''} | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context | r.status_code == 201 OR r.status_code == 400 | Passed |
| BU-BRD-06 | Brand exceptions | 1. Run `tests/test_brands_route.py::TestBrands::test_brand_exceptions` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/brands', json={'name': 'X'}; PUT f'/api/brands/{b.brand_id}', json={'name': 'X'}; DELETE f'/api/brands/{b.brand_id}'; Seed/input objects: Brand(name='T') | brands blueprint registered; SQLite Brand table active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.brands.db.session.commit; Mock/setup values: mc.side_effect = Exception('err'); SQLite DB seeded and committed/flushed before request where the test body does so | c.post('/api/brands', json={'name': 'X'}).status_code == 500; c.put(f'/api/brands/{b.brand_id}', json={'name': 'X'}).status_code == 500; c.delete(f'/api/brands/{b.brand_id}').status_code == 500 | Passed |
| BU-BFS-01 | Bulk add all iterates products | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_bulk_add_all_iterates_products` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/bulk-faiss/add-all', json={'wait_duration_seconds': 0}; Seed/input objects: Brand(name='Test Brand'); Product(name='Test Product', price=10.0, brand_id=brand.brand_id) | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; time.sleep patched to return immediately; patched FAISS service returning deterministic product/search results; active Flask application context with test DB tables available; Flask test client inside app context; Patched: routes.bulk_faiss.faiss_service; time.sleep; Mock/setup values: mock_faiss.get_available_models.return_value = {'status': 'success', 'data': {'defaults': {'textual': 'ViT-B/32', 'visual': 'ViT-B/32'}}}; mock_faiss.get_available_model_ids.return_value = ['ViT-B/32']; mock_faiss.clear_index.return_value = {'status': 'success', 'details': {}}; mock_faiss.add_product.return_value = {'status': 'success', 'details': {'textual_vector_id': 'abc', 'visual_vector_ids': ['v1']}}; SQLite DB seeded and committed/flushed before request where the test body does so | response.status_code == 200; data['details']['successful_count'] >= 1 | Passed |
| BU-BFS-02 | Bulk rebuild with test clears and readds | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_bulk_rebuild_with_test_clears_and_readds` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/bulk-faiss/rebuild-with-test', json={'wait_duration_seconds': 0, 'wait_after_first': False}; Seed/input objects: Brand(name='Test Brand'); Product(name='Test Product', price=10.0, brand_id=brand.brand_id) | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; time.sleep patched to return immediately; patched FAISS service returning deterministic product/search results; active Flask application context with test DB tables available; Flask test client inside app context; Patched: routes.bulk_faiss.faiss_service; time.sleep; Mock/setup values: mock_faiss.get_available_models.return_value = {'status': 'success', 'data': {'defaults': {'textual': 'ViT-B/32', 'visual': 'ViT-B/32'}}}; mock_faiss.get_available_model_ids.return_value = ['ViT-B/32']; mock_faiss.add_test_product.return_value = {'status': 'success', 'details': {}}; mock_faiss.add_product.return_value = {'status': 'success', 'details': {'textual_vector_id': 'abc', 'visual_vector_ids': ['v1']}}; SQLite DB seeded and committed/flushed before request where the test body does so | response.status_code == 200; 'steps' in data | Passed |
| BU-BFS-03 | Bulk stats returns import statistics | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_bulk_stats_returns_import_statistics` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/stats'; Seed/input objects: Brand(name='Stats Brand'); Product(name='Stats Product', price=10.0, brand_id=brand.brand_id) | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; requests.get patched by decorator; active Flask application context with test DB tables available; Flask test client inside app context; Patched: requests.get; Mock/setup values: mock_response.status_code = 200; mock_get.return_value = mock_response; SQLite DB seeded and committed/flushed before request where the test body does so | response.status_code == 200; 'total_products' in data; 'total_images' in data; 'faiss_available' in data | Passed |
| BU-BFS-04 | Models endpoint | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_models_endpoint` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/models' | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; active Flask application context with test DB tables available; Flask test client inside app context | r.status_code == 200; 'models' in data; 'defaults' in data | Passed |
| BU-BFS-05 | Stats faiss unavailable | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_stats_faiss_unavailable` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/stats' | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; active Flask application context with test DB tables available; Flask test client inside app context; Patched: requests.get; Mock/setup values: mock_get.side_effect = Exception('refused') | r.status_code == 200; r.get_json()['faiss_available'] is False | Passed |
| BU-BFS-06 | Web ui page | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_web_ui_page` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/' | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; active Flask application context with test DB tables available; Flask test client inside app context | r.status_code == 200; 'text/html' in r.content_type | Passed |
| BU-BFS-07 | Stats faiss unavailable exception | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_stats_faiss_unavailable_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/stats' | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; active Flask application context with test DB tables available; Flask test client inside app context; Patched: requests.get; Mock/setup values: mg.side_effect = Exception('err') | r.status_code == 200; r.get_json()['faiss_available'] is False | Passed |
| BU-BFS-08 | Web ui contains models | 1. Run `tests/test_bulk_faiss.py::TestBulkFAISS::test_web_ui_contains_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/bulk-faiss/' | bulk_faiss blueprint registered; Product and Brand rows can be seeded before requests; active Flask application context with test DB tables available; Flask test client inside app context | 'ViT-B/32' in html | Passed |
| BU-CAT-01 | Crud | 1. Run `tests/test_categories_route.py::TestCategories::test_crud` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'Root'}; GET f'/api/categories/{p_id}'; PUT f'/api/categories/{p_id}', json={'name': 'Renamed'}; DELETE f'/api/categories/{p_id}'; GET '/api/categories' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.post('/api/categories', json={'name': 'Root'}).status_code == 201; c.get(f'/api/categories/{p_id}').status_code == 200; c.put(f'/api/categories/{p_id}', json={'name': 'Renamed'}).status_code == 200; c.delete(f'/api/categories/{p_id}').status_code == 200 | Passed |
| BU-CAT-02 | Subcategory | 1. Run `tests/test_categories_route.py::TestCategories::test_subcategory` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'Parent'}; POST '/api/categories', json={'name': 'Child', 'parent_category_id': pid} | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | r.status_code == 201; r.get_json()['parent_category_id'] == pid | Passed |
| BU-CAT-03 | Tree | 1. Run `tests/test_categories_route.py::TestCategories::test_tree` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'P'}; POST '/api/categories', json={'name': 'C', 'parent_category_id': pid}; GET '/api/categories?tree=true' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | any((x['name'] == 'P'; 'children' in x for x in r.get_json()['categories'])) | Passed |
| BU-CAT-04 | Parent filter | 1. Run `tests/test_categories_route.py::TestCategories::test_parent_filter` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'P'}; POST '/api/categories', json={'name': 'C', 'parent_category_id': pid}; GET f'/api/categories?parent_id={pid}' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | len(c.get(f'/api/categories?parent_id={pid}').get_json()['categories']) == 1 | Passed |
| BU-CAT-05 | Validation | 1. Run `tests/test_categories_route.py::TestCategories::test_validation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={}; PUT '/api/categories/99999', json={'name': 'X'}; DELETE '/api/categories/99999'; POST '/api/categories', json={'name': 'X', 'parent_category_id': 99999} | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.post('/api/categories', json={}).status_code == 400; c.put('/api/categories/99999', json={'name': 'X'}).status_code == 404; c.delete('/api/categories/99999').status_code == 404; c.post('/api/categories', json={'name': 'X', 'parent_category_id': 99999}).status_code == 400 | Passed |
| BU-CAT-06 | Suicide parent | 1. Run `tests/test_categories_route.py::TestCategories::test_suicide_parent` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'L'}; PUT f'/api/categories/{pid}', json={'parent_category_id': pid} | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.put(f'/api/categories/{pid}', json={'parent_category_id': pid}).status_code == 400 | Passed |
| BU-CAT-07 | Update category not found | 1. Run `tests/test_categories_route.py::TestCategories::test_update_category_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT '/api/categories/99999', json={'name': 'X'} | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.put('/api/categories/99999', json={'name': 'X'}).status_code == 404 | Passed |
| BU-CAT-08 | Delete category not found | 1. Run `tests/test_categories_route.py::TestCategories::test_delete_category_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE '/api/categories/99999' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.delete('/api/categories/99999').status_code == 404 | Passed |
| BU-CAT-09 | Get category exception | 1. Run `tests/test_categories_route.py::TestCategories::test_get_category_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/categories/1' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.categories.Category; Mock/setup values: mc.query.get.side_effect = Exception('err') | c.get('/api/categories/1').status_code == 500 | Passed |
| BU-CAT-10 | Create category exception | 1. Run `tests/test_categories_route.py::TestCategories::test_create_category_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'X'} | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.categories.db.session.add; Mock/setup values: ma.side_effect = Exception('err') | c.post('/api/categories', json={'name': 'X'}).status_code == 500 | Passed |
| BU-CAT-11 | Update category not found parent | 1. Run `tests/test_categories_route.py::TestCategories::test_update_category_not_found_parent` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/categories/{cat.category_id}', json={'parent_category_id': 99999}; Seed/input objects: Category(name='C') | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | r.status_code == 400 | Passed |
| BU-CAT-12 | Get category not found | 1. Run `tests/test_categories_route.py::TestCategories::test_get_category_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/categories/99999' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | c.get('/api/categories/99999').status_code == 404 | Passed |
| BU-CAT-13 | Delete with children | 1. Run `tests/test_categories_route.py::TestCategories::test_delete_with_children` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/categories', json={'name': 'P'}; POST '/api/categories', json={'name': 'C', 'parent_category_id': pid}; DELETE f'/api/categories/{pid}' | categories blueprint registered; SQLite Category table active; active Flask application context with test DB tables available; module Flask test client inside app context | db.session.get(Category, cid) is not None | Passed |
| BU-COR-01 | Models error | 1. Run `tests/test_correction.py::TestCorrection::test_models_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/correction/models' | correction route uses mocked text correction service and Flask test client; pytest fixture `mg`; module Flask test client inside app context; Patched: routes.correction.text_corrector_service.get_available_models; Mock/setup values: mg.return_value = {'status': 'error', 'error': 'x'} | c.get('/api/correction/models').status_code == 500 | Passed |
| BU-COR-02 | Models | 1. Run `tests/test_correction.py::TestCorrection::test_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/correction/models' | correction route uses mocked text correction service and Flask test client; pytest fixture `mg`; module Flask test client inside app context; Patched: routes.correction.text_corrector_service.get_available_models; Mock/setup values: mg.return_value = {'status': 'success', 'data': {'engines': []}, 'source': 'local'} | c.get('/api/correction/models').status_code == 200 | Passed |
| BU-COR-03 | Models exception | 1. Run `tests/test_correction.py::TestCorrection::test_models_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/correction/models' | correction route uses mocked text correction service and Flask test client; pytest fixture `mg`; module Flask test client inside app context; Patched: routes.correction.text_corrector_service.get_available_models; Mock/setup values: mg.side_effect = Exception('err') | c.get('/api/correction/models').status_code == 500 | Passed |
| BU-COR-04 | Save engine | 1. Run `tests/test_correction.py::TestCorrection::test_save_engine` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/correction/selected-engine/save', json={'engine': 's'}; POST '/api/correction/selected-engine/save', json={} | correction route uses mocked text correction service and Flask test client; pytest fixture `ms`; module Flask test client inside app context; Patched: routes.correction.text_corrector_service.save_selected_engine; Mock/setup values: ms.return_value = {'status': 'success', 'message': 'ok', 'data': {'engine': 's'}} | c.post('/api/correction/selected-engine/save', json={'engine': 's'}).status_code == 200; c.post('/api/correction/selected-engine/save', json={}).status_code == 400 | Passed |
| BU-FRS-01 | Text search sends request | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_text_search_sends_request` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_text('query') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': [{'product_id': 1}]}) | r['status'] == 'success'; mp.call_args[1]['json']['text'] == 'query' | Passed |
| BU-FRS-02 | Text search error paths | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_text_search_error_paths` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_text('q') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout(); mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr; 1 additional mock/setup values | svc().search_text('q')['status'] == 'error' | Passed |
| BU-FRS-03 | Search image | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image('/tmp/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': []}) | svc().search_image('/tmp/t.jpg')['status'] == 'success' | Passed |
| BU-FRS-04 | Search image not found | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_image_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image('/x.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: os.path.exists | svc().search_image('/x.jpg')['status'] == 'error' | Passed |
| BU-FRS-05 | Early fusion | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_early_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_early_fusion(text='q', image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': []}) | svc().search_early_fusion(text='q', image_path='/t.jpg')['status'] == 'success' | Passed |
| BU-FRS-06 | Late fusion | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_late_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_late_fusion(text='q', image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': []}) | svc().search_late_fusion(text='q', image_path='/t.jpg')['status'] == 'success' | Passed |
| BU-FRS-07 | Image by text | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_image_by_text` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image_by_text(text='q') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': []}) | svc().search_image_by_text(text='q')['status'] == 'success' | Passed |
| BU-FRS-08 | Text by image | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_text_by_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_text_by_image(image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'products': []}) | svc().search_text_by_image(image_path='/t.jpg')['status'] == 'success' | Passed |
| BU-FRS-09 | Add product success and errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_product_success_and_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc_inst.add_product(product_id='', name='T', description='', brand='B', category='C', price=1, images=[]); svc_inst.add_product(product_id='1', name='', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads | r['status'] == 'error' | Passed |
| BU-FRS-10 | Update product success and errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_update_product_success_and_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc_inst.update_product(product_id='', name='T', description='', brand='', category='', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads | svc_inst.update_product(product_id='', name='T', description='', brand='', category='', price=1, images=[])['status'] == 'error' | Passed |
| BU-FRS-11 | Add update product http errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_update_product_http_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().add_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]); svc().update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: f"services.faiss_retrieval_service.requests.{('post' if method == 'add' else 'put')}; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout(); mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr | r['status'] == 'error' | Passed |
| BU-FRS-12 | Delete product errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_delete_product_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().delete_product('1') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.delete; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout(); mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr | r['status'] in ('error', 'success') | Passed |
| BU-FRS-13 | Clear index | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_clear_index` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().clear_index() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.delete; Mock/setup values: mr.status_code = status_code; mr.json.return_value = {'deleted_count': 10}; mp.return_value = mr | r['status'] == 'error'; r['status'] == 'success' | Passed |
| BU-FRS-14 | Health check | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_health_check` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().health_check() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mr.status_code = 200 if status == 'healthy' else 503; mr.json.return_value = {'status': status}; mp.return_value = mr | svc().health_check()['status'] == status | Passed |
| BU-FRS-15 | Health check connection error | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_health_check_connection_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().health_check() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mp.side_effect = ConnectionError() | svc().health_check()['status'] == 'unhealthy' | Passed |
| BU-FRS-16 | Get available models | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_available_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_available_models() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mr.status_code = 200; mr.json.return_value = {'data': {'textual_models': [{'name': 'ViT-B/32'}], 'visual_models': []}}; mp.return_value = mr | svc().get_available_models()['status'] == 'success' | Passed |
| BU-FRS-17 | Get available models fallback | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_available_models_fallback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_available_models() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mp.side_effect = ConnectionError() | r['source'] == 'local_config' | Passed |
| BU-FRS-18 | Get index stats | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_index_stats` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_index_stats() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mr.status_code = 200; mr.json.return_value = {'indices': {}}; mp.return_value = mr | svc().get_index_stats()['status'] == 'success' | Passed |
| BU-FRS-19 | Search early fusion missing text | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_early_fusion_missing_text` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_early_fusion(text='', image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: os.path.exists | r['status'] == 'error' | Passed |
| BU-FRS-20 | Search late fusion no text | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_late_fusion_no_text` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_late_fusion(text='', image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads | r['status'] == 'error' | Passed |
| BU-FRS-21 | Search image by text empty | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_image_by_text_empty` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image_by_text(text='') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads | svc().search_image_by_text(text='')['status'] == 'error' | Passed |
| BU-FRS-22 | Search text by image not found | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_text_by_image_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_text_by_image('/x.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: os.path.exists | svc().search_text_by_image('/x.jpg')['status'] == 'error' | Passed |
| BU-FRS-23 | Add product skipped | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_product_skipped` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().add_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; flask.current_app; Mock/setup values: ca.config.get.return_value = '/tmp'; mr.status_code = 200; mr.json.return_value = {'status': 'success', 'details': {'skipped': True}}; mp.return_value = mr | r['status'] == 'skipped' | Passed |
| BU-FRS-24 | Search generic connection error | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_generic_connection_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search('q') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.side_effect = ConnectionError() | svc().search('q')['success'] is False | Passed |
| BU-FRS-25 | Get available model ids fallback | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_available_model_ids_fallback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.get_available_model_ids() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: patch.object(s, 'get_available_models'); Mock/setup values: m.return_value = {'data': {}} | len(ids) > 0 | Passed |
| BU-FRS-26 | Update product faiss error | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_update_product_faiss_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.put; Mock/setup values: mr.status_code = 200; mr.json.return_value = {'status': 'error', 'error': 'x'}; mp.return_value = mr | svc().update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[])['status'] == 'error' | Passed |
| BU-FRS-27 | All error paths | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_all_error_paths` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET 'status'; GET 'success' | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout(); mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr | r.get('status') == 'error' OR r.get('success') is False | Passed |
| BU-FRS-28 | Empty inputs | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_empty_inputs` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.search_text(''); s.search_image(''); s.search_early_fusion(text='', image_path='/t.jpg'); s.search_late_fusion(text='', image_path='/t.jpg'); s.search_image_by_text(text=''); s.search_text_by_image(image_path='') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads | s.search_text('')['status'] == 'error'; s.search_image('')['status'] == 'error'; s.search_early_fusion(text='', image_path='/t.jpg')['status'] == 'error'; s.search_late_fusion(text='', image_path='/t.jpg')['status'] == 'error'; s.search_image_by_text(text='')['status'] == 'error'; s.search_text_by_image(image_path='')['status'] == 'error' | Passed |
| BU-FRS-29 | Add product error handler | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_product_error_handler` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().add_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout() | r['status'] == 'error' | Passed |
| BU-FRS-30 | Add product with images | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_product_with_images` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().add_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=['/img.jpg']) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; flask.current_app; os.path.exists; Mock/setup values: ca.config.get.return_value = '/tmp'; mr.status_code = 200; mr.json.return_value = {'status': 'success', 'details': {'textual_vector_id': 't1', 'visual_vector_ids': ['v1']}}; mp.return_value = mr | r['status'] == 'success' | Passed |
| BU-FRS-31 | More error paths | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_more_error_paths` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.get_available_models(); s.search_early_fusion(text='q', image_path='/t.jpg'); s.search_late_fusion(text='q', image_path='/t.jpg'); s.search_text_by_image(image_path='/t.jpg'); s.search_image('/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; services.faiss_retrieval_service.requests.get; os.path.exists; Mock/setup values: mp.side_effect = ConnectionError() | s.get_available_models()['source'] == 'local_config'; s.search_early_fusion(text='q', image_path='/t.jpg')['status'] == 'error'; s.search_late_fusion(text='q', image_path='/t.jpg')['status'] == 'error'; s.search_text_by_image(image_path='/t.jpg')['status'] == 'error'; s.search_image('/t.jpg')['status'] == 'error' | Passed |
| BU-FRS-32 | Update delete product errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_update_delete_product_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]); s.search_text('q'); s.health_check(); s.get_index_stats(); s.get_available_models() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.put; services.faiss_retrieval_service.HAS_REQUESTS -> False; Mock/setup values: mp.side_effect = ConnectionError(); mr.status_code = 200; mr.json.return_value = {'status': 'error', 'error': 'x'}; mp.return_value = mr | r['status'] == 'error'; s.update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[])['status'] == 'error'; s.search_text('q')['status'] == 'error'; s.health_check()['status'] == 'unhealthy'; s.get_index_stats()['status'] == 'error'; s.get_available_models()['status'] == 'success' | Passed |
| BU-FRS-33 | Save selected models request | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_save_selected_models_request` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().save_selected_models('ViT-B/32', 'ViT-B/32') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mr.status_code = 200; mr.json.return_value = {'status': 'success', 'data': {}}; mp.return_value = mr | svc().save_selected_models('ViT-B/32', 'ViT-B/32')['status'] == 'success' | Passed |
| BU-FRS-34 | Add test product | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_test_product` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.add_test_product('test-001') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: patch.object(s, 'add_product') | s.add_test_product('test-001')['status'] == 'success' | Passed |
| BU-FRS-35 | Get available model ids | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_available_model_ids` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_available_model_ids() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: patch.object(svc(), 'get_available_models'); Mock/setup values: m.return_value = {'data': {'textual_models': [{'id': 'ViT-B/32'}]}} | 'ViT-B/32' in svc().get_available_model_ids() | Passed |
| BU-FRS-36 | Search image non 200 | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_image_non_200` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image('/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr | svc().search_image('/t.jpg')['status'] == 'error' | Passed |
| BU-FRS-37 | Search with different errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_search_with_different_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search_image_by_text(text='q'); GET 'status'; GET 'success'; svc().search_text_by_image(image_path='/t.jpg'); svc().search_early_fusion(text='q', image_path='/t.jpg'); svc().search('q'); svc().search_late_fusion(text='q', image_path='/t.jpg') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; os.path.exists; Mock/setup values: mr.status_code = 500; mr.json.return_value = {'error': 'x'}; mp.return_value = mr | r.get('status') == 'error' OR r.get('success') is False | Passed |
| BU-FRS-38 | Add product errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_add_product_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().add_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.HAS_REQUESTS -> error == 'no_reqs' if error == 'no_reqs' else True; services.faiss_retrieval_service.requests.post; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout() | r['status'] in ('error', 'success') | Passed |
| BU-FRS-39 | Update product errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_update_product_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().update_product(product_id='1', name='T', description='', brand='B', category='C', price=1, images=[]) | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.put; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout() | r['status'] in ('error', 'success') | Passed |
| BU-FRS-40 | Get available models non 200 | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_available_models_non_200` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_available_models() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.get; Mock/setup values: mr.status_code = 500; mp.return_value = mr | svc().get_available_models()['source'] == 'local_config' | Passed |
| BU-FRS-41 | Get index stats errors | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_get_index_stats_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().get_index_stats() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.HAS_REQUESTS -> False; services.faiss_retrieval_service.requests.get; Mock/setup values: mp.side_effect = ConnectionError() | svc().get_index_stats()['status'] == 'error' | Passed |
| BU-FRS-42 | No requests fallbacks | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_no_requests_fallbacks` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().search('q'); svc().search_text('q'); svc().health_check(); svc().get_index_stats() | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.HAS_REQUESTS -> False | svc().search('q')['success'] is False; svc().search_text('q')['status'] == 'error'; svc().health_check()['status'] == 'unhealthy'; svc().get_index_stats()['status'] == 'error' | Passed |
| BU-FRS-43 | Save selected models | 1. Run `tests/test_faiss_retrieval_service.py::TestFAISSRetrievalService::test_save_selected_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: svc().save_selected_models('ViT-B/32', 'ViT-B/32') | FAISSRetrievalService runs under Flask app context and external HTTP calls are mocked; minimal Flask app context with UPLOAD_FOLDER=/tmp/test_uploads; Patched: services.faiss_retrieval_service.requests.post; Mock/setup values: mp.return_value = _mock_post_200({'status': 'success', 'data': {}}) | svc().save_selected_models('ViT-B/32', 'ViT-B/32')['status'] == 'success' | Passed |
| BU-FDB-01 | Feedback | 1. Run `tests/test_feedback_route.py::TestFeedback::test_feedback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/feedback', json={**data, 'is_relevant': True}; POST '/api/feedback', json={}; POST '/api/feedback', json={'query_id': 99999, 'product_id': 99999, 'is_relevant': True} | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `data` | c.post('/api/feedback', json={**data, 'is_relevant': True}).status_code == 200; c.post('/api/feedback', json={}).status_code == 400; c.post('/api/feedback', json={'query_id': 99999, 'product_id': 99999, 'is_relevant': True}).status_code == 404 | Passed |
| BU-FDB-02 | Click | 1. Run `tests/test_feedback_route.py::TestFeedback::test_click` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/click', json=data; POST '/api/click', json={}; POST '/api/click', json={'query_id': 99999, 'product_id': 99999} | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `data` | c.post('/api/click', json=data).status_code == 200; c.post('/api/click', json={}).status_code == 400; c.post('/api/click', json={'query_id': 99999, 'product_id': 99999}).status_code == 404 | Passed |
| BU-FDB-03 | Feedback exception | 1. Run `tests/test_feedback_route.py::TestFeedback::test_feedback_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/feedback', json={**data, 'is_relevant': True}; POST '/api/click', json=data | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `data`; Patched: routes.feedback.db.session.commit; Mock/setup values: mc.side_effect = Exception('err') | c.post('/api/feedback', json={**data, 'is_relevant': True}).status_code == 500; c.post('/api/click', json=data).status_code == 500 | Passed |
| BU-FDB-04 | Metrics exception | 1. Run `tests/test_feedback_route.py::TestFeedback::test_metrics_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/metrics' | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.feedback.SearchQuery; Mock/setup values: ms.query.count.side_effect = Exception('err') | c.get('/api/metrics').status_code == 500 | Passed |
| BU-FDB-05 | Metrics zero data | 1. Run `tests/test_feedback_route.py::TestFeedback::test_metrics_zero_data` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/metrics' | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context | r.status_code == 200; r.get_json()['total_searches'] == 0 | Passed |
| BU-FDB-06 | Metrics | 1. Run `tests/test_feedback_route.py::TestFeedback::test_metrics` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/metrics' | feedback blueprint registered; Search/Retrieve feedback rows available through SQLite; active Flask application context with test DB tables available; module Flask test client inside app context; pytest fixture `data` | r.status_code == 200; d['total_searches'] >= 1; 'click_through_rate' in d | Passed |
| BU-HLT-01 | Health db reachable | 1. Run `tests/test_health.py::test_health_db_reachable` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/health' | health blueprint registered through the shared Flask TestingConfig app; Flask test client inside app context | response.status_code == 200; data['status'] == 'healthy'; 'database' in data | Passed |
| BU-HLT-02 | Health db unreachable | 1. Run `tests/test_health.py::test_health_db_unreachable` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/health' | health blueprint registered through the shared Flask TestingConfig app; Flask test client inside app context; Patched: routes.health.db.session.execute; Mock/setup values: mock_exec.side_effect = Exception('DB unavailable') | response.status_code == 503; 'unhealthy' in data['database'] | Passed |
| BU-IMG-01 | Save uploaded image | 1. Run `tests/test_image_helpers.py::test_save_uploaded_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: url and url.startswith('/uploads/products/'); save_uploaded_image(f2, 1) is None | image helper functions run with temp files/base64 strings and patched upload config; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB; active Flask application context with test DB tables available | url; url.startswith('/uploads/products/'); save_uploaded_image(f2, 1) is None | Passed |
| BU-IMG-02 | Save base64 image | 1. Run `tests/test_image_helpers.py::test_save_base64_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: save_base64_image(f'data:image/png;base64,{d}', product_id=1); save_base64_image('bad', product_id=1) | image helper functions run with temp files/base64 strings and patched upload config; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB; active Flask application context with test DB tables available | save_base64_image(f'data:image/png;base64,{d}', product_id=1) is not None; save_base64_image('bad', product_id=1) is None | Passed |
| BU-IMG-03 | Get image as base64 | 1. Run `tests/test_image_helpers.py::test_get_image_as_base64` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: r and r.startswith('data:image/') | image helper functions run with temp files/base64 strings and patched upload config; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB; active Flask application context with test DB tables available | r; r.startswith('data:image/') | Passed |
| BU-IMG-04 | Allowed file | 1. Run `tests/test_image_helpers.py::test_allowed_file` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: allowed_file(f'f.{ext}'); allowed_file('f.txt'); allowed_file('f') | image helper functions run with temp files/base64 strings and patched upload config; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB; active Flask application context with test DB tables available | not allowed_file('f.txt'); (not allowed_file('f')); allowed_file(f'f.{ext}') | Passed |
| BU-MCF-01 | Resolve fused | 1. Run `tests/test_model_config.py::test_resolve_fused` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: resolve_fused_model('ViT-L/14', 'ViT-L/14', fused_model='ViT-B/32'); resolve_fused_model('A', 'B', fused_model='X'); resolve_fused_model('A', 'B', fallback='F') | config.models functions use in-memory constants or mocked selected_models.json I/O | resolve_fused_model('ViT-L/14', 'ViT-L/14', fused_model='ViT-B/32') == 'ViT-L/14'; resolve_fused_model('A', 'B', fused_model='X') == 'X'; resolve_fused_model('A', 'B', fallback='F') == 'F' | Passed |
| BU-MCF-02 | Is valid model | 1. Run `tests/test_model_config.py::test_is_valid_model` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: is_valid_model(mid); is_valid_model('x') | config.models functions use in-memory constants or mocked selected_models.json I/O | not is_valid_model('x'); is_valid_model(mid) | Passed |
| BU-MCF-03 | Get model info | 1. Run `tests/test_model_config.py::test_get_model_info` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_model_info('ViT-B/32') | config.models functions use in-memory constants or mocked selected_models.json I/O | i['name']; i['dimension'] == 512 | Passed |
| BU-MCF-04 | Get model info non dict | 1. Run `tests/test_model_config.py::test_get_model_info_non_dict` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_model_info('garbage') | config.models functions use in-memory constants or mocked selected_models.json I/O | r is not None | Passed |
| BU-MCF-05 | Defaults | 1. Run `tests/test_model_config.py::test_defaults` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: DEFAULT_TEXTUAL_MODEL == 'BAAI/bge-large-en-v1.5'; DEFAULT_VISUAL_MODEL == 'ViT-B/32' | config.models functions use in-memory constants or mocked selected_models.json I/O | DEFAULT_TEXTUAL_MODEL == 'BAAI/bge-large-en-v1.5'; DEFAULT_VISUAL_MODEL == 'ViT-B/32' | Passed |
| BU-MCF-06 | Get models list | 1. Run `tests/test_model_config.py::test_get_models_list` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_models_list() | config.models functions use in-memory constants or mocked selected_models.json I/O | len(get_models_list()) == len(AVAILABLE_MODELS) | Passed |
| BU-MCF-07 | Get model options html | 1. Run `tests/test_model_config.py::test_get_model_options_html` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_model_options_html() | config.models functions use in-memory constants or mocked selected_models.json I/O | DEFAULT_TEXTUAL_MODEL in h; 'selected' in h; h.startswith('<option'); 'Varsayılan' in h | Passed |
| BU-MCF-08 | Get selected models | 1. Run `tests/test_model_config.py::test_get_selected_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_selected_models() | config.models functions use in-memory constants or mocked selected_models.json I/O | 'textual_model' in r; 'fusion_endpoint' in r | Passed |
| BU-MCF-09 | Get selected models file not found | 1. Run `tests/test_model_config.py::test_get_selected_models_file_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_selected_models() | config.models functions use in-memory constants or mocked selected_models.json I/O; Patched: builtins.open; Mock/setup values: mo.side_effect = FileNotFoundError() | r['textual_model'] == DEFAULT_TEXTUAL_MODEL | Passed |
| BU-MCF-10 | Save selected models | 1. Run `tests/test_model_config.py::test_save_selected_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: save_selected_models('ViT-L/14', 'ViT-L/14', 'late', 'ViT-B/32') | config.models functions use in-memory constants or mocked selected_models.json I/O; Patched: config.models.get_selected_models; config.models.resolve_fused_model; builtins.open -> mock_open() | mf.assert_called_once() | Passed |
| BU-MCF-11 | Save selected fusion endpoint | 1. Run `tests/test_model_config.py::test_save_selected_fusion_endpoint` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: save_selected_fusion_endpoint('early') | config.models functions use in-memory constants or mocked selected_models.json I/O; Patched: config.models.get_selected_models; config.models.save_selected_models | ms.assert_called_once() | Passed |
| BU-MCF-12 | Get selected fusion endpoint | 1. Run `tests/test_model_config.py::test_get_selected_fusion_endpoint` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_selected_fusion_endpoint() | config.models functions use in-memory constants or mocked selected_models.json I/O; Patched: config.models.get_selected_models | get_selected_fusion_endpoint() == 'early'; get_selected_fusion_endpoint() in ('late', 'early') | Passed |
| BU-MCF-13 | Get model options html descriptions | 1. Run `tests/test_model_config.py::test_get_model_options_html_descriptions` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: get_model_options_html() | config.models functions use in-memory constants or mocked selected_models.json I/O | True | Passed |
| BU-MCF-14 | Get selected models with existing config | 1. Run `tests/test_model_config.py::test_get_selected_models_with_existing_config` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Input variables: data = '{"textual_model":"ViT-B/16","visual_model":"ViT-B/16"}'; Calls: get_selected_models() | config.models functions use in-memory constants or mocked selected_models.json I/O; Patched: builtins.open -> mock_open(read_data=data) | r['textual_model'] == 'ViT-B/16' | Passed |
| BU-MOD-01 | Product to dict all fields | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_all_fields` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(); Seed/input objects: Product(name='Test Product', price=99.99, description='A test') | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | result['product_id'] == product.product_id; result['name'] == 'Test Product'; result['price'] == 99.99 | Passed |
| BU-MOD-02 | Product to dict include brand true | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_brand_true` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_brand=True); Seed/input objects: Brand(name='Test Brand'); Product(name='Branded Product', price=49.99, brand_id=brand.brand_id) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'brand' in result; result['brand']['name'] == 'Test Brand' | Passed |
| BU-MOD-03 | Product to dict include brand false | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_brand_false` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_brand=False); Seed/input objects: Brand(name='Hidden Brand'); Product(name='No Brand Shown', price=10.0, brand_id=brand.brand_id) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'brand' not in result | Passed |
| BU-MOD-04 | Product to dict include images true | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_images_true` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_images=True); Seed/input objects: Product(name='Product With Images', price=25.0); ProductImage(product_id=product.product_id, url='/uploads/products/test.jpg') | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'images' in result; len(result['images']) > 0 | Passed |
| BU-MOD-05 | Product to dict include images false | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_images_false` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_images=False); Seed/input objects: Product(name='Product No Images', price=15.0); ProductImage(product_id=product.product_id, url='/uploads/products/test.jpg') | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'images' not in result | Passed |
| BU-MOD-06 | Product to dict include categories true | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_categories_true` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_categories=True); Seed/input objects: Category(name='Electronics'); Product(name='Gadget', price=199.99) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'categories' in result; len(result['categories']) > 0 | Passed |
| BU-MOD-07 | Product to dict include categories false | 1. Run `tests/test_models.py::TestProductToDict::test_product_to_dict_include_categories_false` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: product.to_dict(include_categories=False); Seed/input objects: Category(name='Books'); Product(name='Novel', price=12.99) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'categories' not in result | Passed |
| BU-MOD-08 | Category to dict flat | 1. Run `tests/test_models.py::TestCategoryToDict::test_category_to_dict_flat` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: cat.to_dict(); Seed/input objects: Category(name='Root Category') | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | result['name'] == 'Root Category'; result['parent_category_id'] is None | Passed |
| BU-MOD-09 | Category to dict with parent | 1. Run `tests/test_models.py::TestCategoryToDict::test_category_to_dict_with_parent` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: child.to_dict(include_parent=True); Seed/input objects: Category(name='Parent'); Category(name='Child', parent_category_id=parent.category_id) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'parent' in result; result['parent']['name'] == 'Parent' | Passed |
| BU-MOD-10 | Category to dict with children | 1. Run `tests/test_models.py::TestCategoryToDict::test_category_to_dict_with_children` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: parent.to_dict(include_children=True); Seed/input objects: Category(name='Family'); Category(name='Member', parent_category_id=parent.category_id) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'children' in result; any((c['name'] == 'Member' for c in result['children'])) | Passed |
| BU-MOD-11 | Category self referencing multi level | 1. Run `tests/test_models.py::TestCategoryToDict::test_category_self_referencing_multi_level` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: child.to_dict(include_parent=True); parent.to_dict(include_parent=True); Seed/input objects: Category(name='Grandparent'); Category(name='Parent', parent_category_id=grandparent.category_id); Category(name='Child', parent_category_id=parent.category_id) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | parent_dict['name'] == 'Parent'; grandparent_dict['parent']['name'] == 'Grandparent' | Passed |
| BU-MOD-12 | Repr | 1. Run `tests/test_models.py::TestSearchQueryModel::test_repr` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Seed/input objects: Product(name='TestProd', price=10.0) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'TestProd' in rep | Passed |
| BU-MOD-13 | To dict basic | 1. Run `tests/test_models.py::TestSearchQueryModel::test_to_dict_basic` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: sq.to_dict(); Seed/input objects: SearchQuery(raw_text='hello', corrected_text='world') | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context | d['raw_text'] == 'hello'; d['corrected_text'] == 'world' | Passed |
| BU-MOD-14 | To dict with results | 1. Run `tests/test_models.py::TestSearchQueryModel::test_to_dict_with_results` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: sq.to_dict(include_results=True); Seed/input objects: SearchQuery(raw_text='test', corrected_text='test'); Product(name='P', price=10.0); Retrieve(search_id=sq.search_id, product_id=p.product_id, rank=1) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'results' in d; len(d['results']) == 1 | Passed |
| BU-MOD-15 | Repr | 1. Run `tests/test_models.py::TestBrandModel::test_repr` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Seed/input objects: Product(name='TestProd', price=10.0) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'TestProd' in rep | Passed |
| BU-MOD-16 | Repr | 1. Run `tests/test_models.py::TestProductModel::test_repr` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Seed/input objects: Product(name='TestProd', price=10.0) | SQLAlchemy model instances are created in the SQLite test database; SQLAlchemy test session from SQLite-backed app context; SQLite DB seeded and committed/flushed before request where the test body does so | 'TestProd' in rep | Passed |
| BU-PRD-01 | List active | 1. Run `tests/test_products_route.py::TestProducts::test_list_active` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/products?is_active=true'; Seed/input objects: _p('A1', is_active=True); _p('A2', is_active=True); _p('I1', is_active=False) | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | r.status_code == 200; r.get_json()['total'] == 2 | Passed |
| BU-PRD-02 | Create success | 1. Run `tests/test_products_route.py::TestProducts::test_create_success` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products', data={'name': 'N', 'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.add_product.return_value = {'status': 'success', 'details': {}} | r.status_code == 201 | Passed |
| BU-PRD-03 | Create validation | 1. Run `tests/test_products_route.py::TestProducts::test_create_validation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products', data={'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST '/api/products', data={'name': 'N', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST '/api/products', data={'name': 'N', 'price': 'abc', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST '/api/products', data={'name': 'N', 'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context | c.post('/api/products', data={'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 400; c.post('/api/products', data={'name': 'N', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 400; c.post('/api/products', data={'name': 'N', 'price': 'abc', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 400; c.post('/api/products', data={'name': 'N', 'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-PRD-04 | Crud | 1. Run `tests/test_products_route.py::TestProducts::test_crud` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products/{p.product_id}'; GET '/api/products/99999'; PUT f'/api/products/{p.product_id}', data={'name': 'X', 'price': '5'}, content_type='multipart/form-data'; DELETE f'/api/products/{p.product_id}'; Seed/input objects: Brand(name='B'); _p('Test', brand_id=b.brand_id) | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.delete_product.return_value = {'status': 'success'}; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products/{p.product_id}').status_code == 200; c.get('/api/products/99999').status_code == 404; c.put(f'/api/products/{p.product_id}', data={'name': 'X', 'price': '5'}, content_type='multipart/form-data').status_code == 200; c.delete(f'/api/products/{p.product_id}').status_code == 200 | Passed |
| BU-PRD-05 | Filters | 1. Run `tests/test_products_route.py::TestProducts::test_filters` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products?category_id={cat.category_id}'; GET f'/api/products?brand_id={b.brand_id}'; GET '/api/products?min_price=1&max_price=20'; Seed/input objects: Category(name='E'); Brand(name='N'); _p('G') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products?category_id={cat.category_id}').get_json()['total'] >= 1; c.get(f'/api/products?brand_id={b.brand_id}').get_json()['total'] >= 1; c.get('/api/products?min_price=1&max_price=20').get_json()['total'] >= 1 | Passed |
| BU-PRD-06 | Image upload | 1. Run `tests/test_products_route.py::TestProducts::test_image_upload` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.post(f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 201; c.post(f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-PRD-07 | Images list and delete | 1. Run `tests/test_products_route.py::TestProducts::test_images_list_and_delete` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products/{p.product_id}/images'; DELETE f'/api/products/{p.product_id}/images/{img.image_no}'; Seed/input objects: _p(); ProductImage(product_id=p.product_id, url='/u/t.jpg') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products/{p.product_id}/images').status_code == 200; c.delete(f'/api/products/{p.product_id}/images/{img.image_no}').status_code == 200 | Passed |
| BU-PRD-08 | Update with images | 1. Run `tests/test_products_route.py::TestProducts::test_update_with_images` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'name': 'U', 'images': (BytesIO(b'x'), 'n.png')}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.add_product.return_value = {'status': 'success', 'details': {}}; m.delete_product.return_value = {'status': 'success'}; SQLite DB seeded and committed/flushed before request where the test body does so | r.status_code == 200 | Passed |
| BU-PRD-09 | Image endpoints | 1. Run `tests/test_products_route.py::TestProducts::test_image_endpoints` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/products/99999/image'; GET '/api/products/99999/images'; Seed/input objects: _p(); ProductImage(product_id=p.product_id, url='/u/t.jpg') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.get('/api/products/99999/image').status_code == 404; c.get('/api/products/99999/images').status_code == 404 | Passed |
| BU-PRD-10 | Upload product image validation | 1. Run `tests/test_products_route.py::TestProducts::test_upload_product_image_validation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products/99999/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.post('/api/products/99999/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 404; c.post(f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-PRD-11 | Delete product not found | 1. Run `tests/test_products_route.py::TestProducts::test_delete_product_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE '/api/products/99999' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context | c.delete('/api/products/99999').status_code == 404 | Passed |
| BU-PRD-12 | Put product not found | 1. Run `tests/test_products_route.py::TestProducts::test_put_product_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT '/api/products/99999', data={'name': 'X'}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context | c.put('/api/products/99999', data={'name': 'X'}, content_type='multipart/form-data').status_code == 404 | Passed |
| BU-PRD-13 | Get products filters | 1. Run `tests/test_products_route.py::TestProducts::test_get_products_filters` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products?category_id={cat.category_id}'; GET f'/api/products?brand_id={b.brand_id}'; GET '/api/products?min_price=1&max_price=20'; Seed/input objects: Category(name='E'); Brand(name='N'); _p('G') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products?category_id={cat.category_id}').get_json()['total'] >= 1; c.get(f'/api/products?brand_id={b.brand_id}').get_json()['total'] >= 1; c.get('/api/products?min_price=1&max_price=20').get_json()['total'] >= 1 | Passed |
| BU-PRD-14 | Put product faiss error ok | 1. Run `tests/test_products_route.py::TestProducts::test_put_product_faiss_error_ok` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'name': 'X'}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.delete_product.return_value = {'status': 'success'}; m.add_product.return_value = {'status': 'error', 'error': 'FAISS err'}; SQLite DB seeded and committed/flushed before request where the test body does so | c.put(f'/api/products/{p.product_id}', data={'name': 'X'}, content_type='multipart/form-data').status_code == 200 | Passed |
| BU-PRD-15 | Put product general exception | 1. Run `tests/test_products_route.py::TestProducts::test_put_product_general_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'name': 'X'}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.db.session.commit; Mock/setup values: mc.side_effect = Exception('err'); SQLite DB seeded and committed/flushed before request where the test body does so | c.put(f'/api/products/{p.product_id}', data={'name': 'X'}, content_type='multipart/form-data').status_code == 500 | Passed |
| BU-PRD-16 | Delete faiss error ok | 1. Run `tests/test_products_route.py::TestProducts::test_delete_faiss_error_ok` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE f'/api/products/{p.product_id}'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.delete_product.return_value = {'status': 'error', 'error': 'FAISS err'}; SQLite DB seeded and committed/flushed before request where the test body does so | c.delete(f'/api/products/{p.product_id}').status_code == 200 | Passed |
| BU-PRD-17 | Delete product general exception | 1. Run `tests/test_products_route.py::TestProducts::test_delete_product_general_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE f'/api/products/{p.product_id}'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.db.session.delete; Mock/setup values: md.side_effect = Exception('err'); SQLite DB seeded and committed/flushed before request where the test body does so | c.delete(f'/api/products/{p.product_id}').status_code == 500 | Passed |
| BU-PRD-18 | Upload image exception | 1. Run `tests/test_products_route.py::TestProducts::test_upload_image_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.os.makedirs; Mock/setup values: mm.side_effect = Exception('IO err'); SQLite DB seeded and committed/flushed before request where the test body does so | r.status_code == 500 | Passed |
| BU-PRD-19 | Delete image not found | 1. Run `tests/test_products_route.py::TestProducts::test_delete_image_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE f'/api/products/{p.product_id}/images/999'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.delete(f'/api/products/{p.product_id}/images/999').status_code == 404 | Passed |
| BU-PRD-20 | Create negative price | 1. Run `tests/test_products_route.py::TestProducts::test_create_negative_price` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products', data={'name': 'N', 'price': '-5', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: mf.add_product.return_value = {'status': 'success', 'details': {}} | r.status_code == 400 | Passed |
| BU-PRD-21 | Update with brand and active | 1. Run `tests/test_products_route.py::TestProducts::test_update_with_brand_and_active` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'brand': 'NewB', 'is_active': 'false'}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.put(f'/api/products/{p.product_id}', data={'brand': 'NewB', 'is_active': 'false'}, content_type='multipart/form-data').status_code == 200; db.session.get(Product, p.product_id).is_active is False | Passed |
| BU-PRD-22 | Upload image validation | 1. Run `tests/test_products_route.py::TestProducts::test_upload_image_validation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products/99999/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data'; POST f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data'; POST f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.post('/api/products/99999/images', data={'file': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data').status_code == 404; c.post(f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data').status_code == 400; c.post(f'/api/products/{p.product_id}/images', data={'file': (BytesIO(b'd'), 't.txt')}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-PRD-23 | Product image endpoints | 1. Run `tests/test_products_route.py::TestProducts::test_product_image_endpoints` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products/{p.product_id}/image'; DELETE f'/api/products/{p.product_id}/images/999'; Seed/input objects: _p(); ProductImage(product_id=p.product_id, url='/uploads/products/t.jpg') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Flask app fixture using TestingConfig, registered blueprints, upload folder, and SQLite test DB; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products/{p.product_id}/image').status_code == 200; c.delete(f'/api/products/{p.product_id}/images/999').status_code == 404 | Passed |
| BU-PRD-24 | Get first image no images | 1. Run `tests/test_products_route.py::TestProducts::test_get_first_image_no_images` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET f'/api/products/{p.product_id}/image'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.get(f'/api/products/{p.product_id}/image').status_code == 404 | Passed |
| BU-PRD-25 | Create invalid brand | 1. Run `tests/test_products_route.py::TestProducts::test_create_invalid_brand` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products', data={'name': 'N', 'price': '10', 'brand': 'B', 'category_ids': 'abc', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: mf.add_product.return_value = {'status': 'success', 'details': {}} | r.status_code == 400 | Passed |
| BU-PRD-26 | Update with category ids | 1. Run `tests/test_products_route.py::TestProducts::test_update_with_category_ids` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'category_ids': str(cat.category_id)}, content_type='multipart/form-data'; Seed/input objects: Category(name='C'); _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | r.status_code == 200 | Passed |
| BU-PRD-27 | Delete faiss error log | 1. Run `tests/test_products_route.py::TestProducts::test_delete_faiss_error_log` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE f'/api/products/{p.product_id}'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.faiss_service; Mock/setup values: m.delete_product.return_value = {'status': 'error', 'error': 'FAISS err'}; SQLite DB seeded and committed/flushed before request where the test body does so | c.delete(f'/api/products/{p.product_id}').status_code == 200 | Passed |
| BU-PRD-28 | Image endpoint not found | 1. Run `tests/test_products_route.py::TestProducts::test_image_endpoint_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: DELETE f'/api/products/{p.product_id}/images/{img.image_no}'; Seed/input objects: _p(); ProductImage(product_id=p.product_id, url='/uploads/products/t.jpg') | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.os.path.exists; SQLite DB seeded and committed/flushed before request where the test body does so | c.delete(f'/api/products/{p.product_id}/images/{img.image_no}').status_code == 200 | Passed |
| BU-PRD-29 | Create product general error | 1. Run `tests/test_products_route.py::TestProducts::test_create_product_general_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/products', data={'name': 'N', 'price': '10', 'brand': 'B', 'images': (BytesIO(b'd'), 't.png')}, content_type='multipart/form-data' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.Product; Mock/setup values: mp.side_effect = Exception('err') | r.status_code == 500 | Passed |
| BU-PRD-30 | Update product invalid price | 1. Run `tests/test_products_route.py::TestProducts::test_update_product_invalid_price` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: PUT f'/api/products/{p.product_id}', data={'price': 'abc'}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.put(f'/api/products/{p.product_id}', data={'price': 'abc'}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-PRD-31 | Get product error | 1. Run `tests/test_products_route.py::TestProducts::test_get_product_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/products/1' | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; Patched: routes.products.db.session.get; Mock/setup values: mg.side_effect = Exception('err') | c.get('/api/products/1').status_code == 500 | Passed |
| BU-PRD-32 | Upload image no file | 1. Run `tests/test_products_route.py::TestProducts::test_upload_image_no_file` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data'; Seed/input objects: _p() | products blueprint registered; SQLite Product/Brand/ProductImage tables and temp upload folder active; active Flask application context with test DB tables available; module Flask test client inside app context; SQLite DB seeded and committed/flushed before request where the test body does so | c.post(f'/api/products/{p.product_id}/images', data={}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-RPX-01 | Search text | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_search_text` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/text', json={'text': 'q'}; POST '/api/retrieval/search/text', json={} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.post('/api/retrieval/search/text', json={'text': 'q'}).status_code == 200; c.post('/api/retrieval/search/text', json={}).status_code == 400 | Passed |
| BU-RPX-02 | Late fusion | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_late_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/late', json={'text': 'q', 'image': '/t.jpg'}; POST '/api/retrieval/search/late', json={'image': '/t.jpg'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.post('/api/retrieval/search/late', json={'text': 'q', 'image': '/t.jpg'}).status_code == 200; c.post('/api/retrieval/search/late', json={'image': '/t.jpg'}).status_code == 400 | Passed |
| BU-RPX-03 | Early fusion | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_early_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/early', json={'text': 'q', 'image': '/t.jpg'}; POST '/api/retrieval/search/early', json={'image': '/t.jpg'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.post('/api/retrieval/search/early', json={'text': 'q', 'image': '/t.jpg'}).status_code == 200; c.post('/api/retrieval/search/early', json={'image': '/t.jpg'}).status_code == 400 | Passed |
| BU-RPX-04 | Stats endpoint | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_stats_endpoint` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/retrieval/stats' | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.get('/api/retrieval/stats').status_code == 200 | Passed |
| BU-RPX-05 | Exception handlers | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_exception_handlers` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/text', json={'text': 'q'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context; Mock/setup values: mock_svc.search_text.side_effect = Exception('err') | c.post('/api/retrieval/search/text', json={'text': 'q'}).status_code == 500 | Passed |
| BU-RPX-06 | Validation errors | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_validation_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/text', json={}; POST '/api/retrieval/add-product', json={'id': '1'}; POST '/api/retrieval/add-product', json={'id': '1', 'name': 'T', 'price': 'bad'}; POST '/api/retrieval/search/text', json={'text': 'q'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context; Mock/setup values: mock_svc.search_text.return_value = {'status': 'error', 'error': 'x'} | c.post('/api/retrieval/search/text', json={}).status_code == 400; c.post('/api/retrieval/add-product', json={'id': '1'}).status_code == 400; c.post('/api/retrieval/add-product', json={'id': '1', 'name': 'T', 'price': 'bad'}).status_code == 400; c.post('/api/retrieval/search/text', json={'text': 'q'}).status_code == 500 | Passed |
| BU-RPX-07 | Image search | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_image_search` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/image', json={'image': '/t.jpg'}; POST '/api/retrieval/search/image', json={} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.post('/api/retrieval/search/image', json={'image': '/t.jpg'}).status_code == 200; c.post('/api/retrieval/search/image', json={}).status_code == 400 | Passed |
| BU-RPX-08 | Add update delete product | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_add_update_delete_product` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/add-product', json={'id': '1', 'name': 'T'}; POST '/api/retrieval/add-product', json={}; PUT '/api/retrieval/update-product/1', json={'name': 'U'}; DELETE '/api/retrieval/delete-product/1' | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.post('/api/retrieval/add-product', json={'id': '1', 'name': 'T'}).status_code == 200; c.post('/api/retrieval/add-product', json={}).status_code == 400; c.put('/api/retrieval/update-product/1', json={'name': 'U'}).status_code == 200; c.delete('/api/retrieval/delete-product/1').status_code == 200 | Passed |
| BU-RPX-09 | Index and models | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_index_and_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/retrieval/index-stats'; GET '/api/retrieval/models'; GET '/api/retrieval/stats'; DELETE '/api/retrieval/clear-index' | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.get('/api/retrieval/index-stats').status_code == 200; c.get('/api/retrieval/models').status_code == 200; c.get('/api/retrieval/stats').status_code == 200; c.delete('/api/retrieval/clear-index').status_code == 200 | Passed |
| BU-RPX-10 | Validation and errors | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_validation_and_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/search/text', data='{}', content_type='application/json'; POST '/api/retrieval/search/late', data='{}', content_type='application/json'; POST '/api/retrieval/search/early', data='{}', content_type='application/json'; POST '/api/retrieval/search/image', data='{}', content_type='application/json'; POST '/api/retrieval/add-product', json={'id': '1'}; POST '/api/retrieval/add-product', json={'id': '1', 'name': 'T', 'price': 'bad'}; POST '/api/retrieval/search/text', json={'text': 'q'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context; Mock/setup values: mock_svc.search_text.return_value = {'status': 'error', 'error': 'x'} | c.post('/api/retrieval/search/text', data='{}', content_type='application/json').status_code == 400; c.post('/api/retrieval/search/late', data='{}', content_type='application/json').status_code == 400; c.post('/api/retrieval/search/early', data='{}', content_type='application/json').status_code == 400; c.post('/api/retrieval/search/image', data='{}', content_type='application/json').status_code == 400; c.post('/api/retrieval/add-product', json={'id': '1'}).status_code == 400; c.post('/api/retrieval/add-product', json={'id': '1', 'name': 'T', 'price': 'bad'}).status_code == 400; c.post('/api/retrieval/search/text', json={'text': 'q'}).status_code == 500 | Passed |
| BU-RPX-11 | Selected models | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_selected_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/selected-models', json={'textual_model': 'ViT-B/32', 'visual_model': 'ViT-B/32'}; GET '/api/retrieval/selected-models' | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context | c.get('/api/retrieval/selected-models').status_code == 200; r.status_code == 200 | Passed |
| BU-RPX-12 | Fusion endpoint | 1. Run `tests/test_retrieval_proxy.py::TestRetrievalProxy::test_fusion_endpoint` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/retrieval/fusion-endpoint'; POST '/api/retrieval/fusion-endpoint', json={}; POST '/api/retrieval/fusion-endpoint', json={'fusion_endpoint': 'bad'}; POST '/api/retrieval/fusion-endpoint', json={'fusion_endpoint': 'early'} | retrieval proxy routes use mocked requests/faiss service responses; pytest fixture `mock_svc`; module Flask test client inside app context; Patched: routes.retrieval.save_selected_fusion_endpoint | c.get('/api/retrieval/fusion-endpoint').status_code == 200; c.post('/api/retrieval/fusion-endpoint', json={}).status_code == 400; c.post('/api/retrieval/fusion-endpoint', json={'fusion_endpoint': 'bad'}).status_code == 400; c.post('/api/retrieval/fusion-endpoint', json={'fusion_endpoint': 'early'}).status_code == 200 | Passed |
| BU-RRT-01 | Save and rebuild accepts model advertised by faiss | 1. Run `tests/test_retrieval_routes.py::test_save_and_rebuild_accepts_model_advertised_by_faiss` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/selected-models/save-and-rebuild', json={'textual_model': model_id, 'visual_model': model_id, 'fusion_endpoint': 'late', 'wait_duration_seconds': 0} | retrieval model-selection routes use TestingConfig and temporary selected model configuration; pytest fixture `mock_save_selected_models`; pytest fixture `mock_get_selected_fusion_endpoint`; pytest fixture `mock_get_available_models`; pytest fixture `mock_clear_index`; pytest fixture `mock_joinedload`; pytest fixture `mock_product`; Flask test client inside app context; Patched: models.product.Product; sqlalchemy.orm.joinedload; routes.retrieval.faiss_service.clear_index; routes.retrieval.faiss_service.get_available_models; routes.retrieval.get_selected_fusion_endpoint; routes.retrieval.save_selected_models; Mock/setup values: mock_get_available_models.return_value = {'status': 'success', 'data': {'textual_models': [{'id': model_id, 'name': model_id}], 'visual_models': [{'id': model_id, 'name': model_id}]}}; mock_clear_index.return_value = {'status': 'success'}; mock_product.query.filter_by.return_value.options.return_value.all.return_value = [] | response.status_code == 404; response.get_json() == {'status': 'error', 'error': 'No products found in database'}; mock_save_selected_models.assert_called_once_with(model_id, model_id, 'late', model_id) | Passed |
| BU-RRT-02 | Selected models accepts fused model advertised by faiss | 1. Run `tests/test_retrieval_routes.py::test_selected_models_accepts_fused_model_advertised_by_faiss` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/selected-models', json={'textual_model': textual_model, 'visual_model': visual_model, 'fused_model': fused_model, 'fusion_endpoint': 'early'} | retrieval model-selection routes use TestingConfig and temporary selected model configuration; pytest fixture `mock_get_available_models`; pytest fixture `mock_save_selected_models`; Flask test client inside app context; Patched: routes.retrieval.save_selected_models; routes.retrieval.faiss_service.get_available_models; Mock/setup values: mock_get_available_models.return_value = {'status': 'success', 'data': {'textual_models': [{'id': textual_model, 'name': textual_model}], 'visual_models': [{'id': visual_model, 'name': visual_model}], 'fused_models': [{'id': fused_model, 'name': fused_model}]}} | response.status_code == 200; response.get_json()['data'] == {'textual_model': textual_model, 'visual_model': visual_model, 'fused_model': fused_model, 'fusion_endpoint': 'early'}; mock_save_selected_models.assert_called_once_with(textual_model, visual_model, 'early', fused_model) | Passed |
| BU-RRT-03 | Selected models sets fused to model when textual and visual match | 1. Run `tests/test_retrieval_routes.py::test_selected_models_sets_fused_to_model_when_textual_and_visual_match` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/selected-models', json={'textual_model': model_id, 'visual_model': model_id, 'fused_model': stale_fused_model, 'fusion_endpoint': 'early'} | retrieval model-selection routes use TestingConfig and temporary selected model configuration; pytest fixture `mock_get_available_models`; pytest fixture `mock_save_selected_models`; pytest fixture `mock_get_selected_models`; Flask test client inside app context; Patched: routes.retrieval.get_selected_models; routes.retrieval.save_selected_models; routes.retrieval.faiss_service.get_available_models; Mock/setup values: mock_get_selected_models.return_value = {'textual_model': 'BAAI/bge-large-en-v1.5', 'visual_model': 'facebook/dinov3-vit7b16-pretrain-lvd1689m', 'fused_model': stale_fused_model, 'fusion_endpoint': 'late'}; mock_get_available_models.return_value = {'status': 'success', 'data': {'textual_models': [{'id': model_id, 'name': model_id}], 'visual_models': [{'id': model_id, 'name': model_id}], 'fused_models': [{'id': stale_fused_model, 'name': stale_fused_model}]}} | response.status_code == 200; response.get_json()['data'] == {'textual_model': model_id, 'visual_model': model_id, 'fused_model': model_id, 'fusion_endpoint': 'early'}; mock_save_selected_models.assert_called_once_with(model_id, model_id, 'early', model_id) | Passed |
| BU-RRT-04 | Add product sets fused to model when textual and visual match | 1. Run `tests/test_retrieval_routes.py::test_add_product_sets_fused_to_model_when_textual_and_visual_match` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/retrieval/add-product', json={'id': 'product-1', 'name': 'Test Product', 'price': 10, 'textual_model_name': model_id, 'visual_model_name': model_id, 'fused_model_name': 'ViT-B/32'} | retrieval model-selection routes use TestingConfig and temporary selected model configuration; pytest fixture `mock_add_product`; Flask test client inside app context; Patched: routes.retrieval.faiss_service.add_product; Mock/setup values: mock_add_product.return_value = {'status': 'success'} | response.status_code == 200; mock_add_product.call_args.kwargs['fused_model_name'] == model_id | Passed |
| BU-SRT-01 | Search text and image | 1. Run `tests/test_search_route.py::TestSearch::test_search_text_and_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't', 'images': (BytesIO(b'f'), 'q.jpg')}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `me`; pytest fixture `ms`; pytest fixture `mf`; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.save_search_image; routes.search.SearchService.execute_search; Mock/setup values: me.return_value = {'search_id': 42}; ms.return_value = 'uploads/products/t.jpg' | r.status_code == 201 | Passed |
| BU-SRT-02 | Search text only | 1. Run `tests/test_search_route.py::TestSearch::test_search_text_only` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't'}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `me`; pytest fixture `mf`; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.SearchService.execute_search; Mock/setup values: me.return_value = {'search_id': 1} | c.post('/api/search', data={'raw_text': 't'}, content_type='multipart/form-data').status_code == 201 | Passed |
| BU-SRT-03 | Search base64 | 1. Run `tests/test_search_route.py::TestSearch::test_search_base64` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't', 'image_base64': b64}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `me`; pytest fixture `mf`; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.SearchService.execute_search; Mock/setup values: me.return_value = {'search_id': 1} | c.post('/api/search', data={'raw_text': 't', 'image_base64': b64}, content_type='multipart/form-data').status_code == 201 | Passed |
| BU-SRT-04 | Search validation | 1. Run `tests/test_search_route.py::TestSearch::test_search_validation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={}, content_type='multipart/form-data'; POST '/api/search', data={'search_mode': 'invalid', 'raw_text': 't'}, content_type='multipart/form-data'; POST '/api/search', data={'search_mode': 'iwt'}, content_type='multipart/form-data'; POST '/api/search', data={'raw_text': 't', 'search_mode': 'twi'}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; module Flask test client inside app context | c.post('/api/search', data={}, content_type='multipart/form-data').status_code == 400; c.post('/api/search', data={'search_mode': 'invalid', 'raw_text': 't'}, content_type='multipart/form-data').status_code == 400; c.post('/api/search', data={'search_mode': 'iwt'}, content_type='multipart/form-data').status_code == 400; c.post('/api/search', data={'raw_text': 't', 'search_mode': 'twi'}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-SRT-05 | Get search | 1. Run `tests/test_search_route.py::TestSearch::test_get_search` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/search/1'; GET '/api/search/999' | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `mg`; module Flask test client inside app context; Patched: routes.search.SearchService.get_search_by_id; Mock/setup values: mg.return_value = {'search_id': 1, 'products': []}; mg.return_value = None | c.get('/api/search/1').status_code == 200; c.get('/api/search/999').status_code == 404 | Passed |
| BU-SRT-06 | Db fallback | 1. Run `tests/test_search_route.py::TestSearch::test_db_fallback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search/db-fallback', json={'search_id': 1}; POST '/api/search/db-fallback', json={} | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `mf`; module Flask test client inside app context; Patched: routes.search.SearchService.execute_db_fallback_search; Mock/setup values: mf.return_value = {'products': []} | c.post('/api/search/db-fallback', json={'search_id': 1}).status_code == 200; c.post('/api/search/db-fallback', json={}).status_code == 400 | Passed |
| BU-SRT-07 | Save search image | 1. Run `tests/test_search_route.py::TestSearch::test_save_search_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Inputs from assertions: p and os.path.exists(p); save_search_image(None) is None; save_search_image(f) is None | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `search_app` | p; os.path.exists(p); save_search_image(None) is None; save_search_image(f) is None | Passed |
| BU-SRT-08 | Search value error returns 404 | 1. Run `tests/test_search_route.py::TestSearch::test_search_value_error_returns_404` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't'}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.SearchService.execute_search; Mock/setup values: me.side_effect = ValueError('invalid') | c.post('/api/search', data={'raw_text': 't'}, content_type='multipart/form-data').status_code == 404 | Passed |
| BU-SRT-09 | Search generic error returns 500 | 1. Run `tests/test_search_route.py::TestSearch::test_search_generic_error_returns_500` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't'}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.SearchService.execute_search; Mock/setup values: me.side_effect = Exception('server err') | c.post('/api/search', data={'raw_text': 't'}, content_type='multipart/form-data').status_code == 500 | Passed |
| BU-SRT-10 | Image validation error | 1. Run `tests/test_search_route.py::TestSearch::test_image_validation_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: POST '/api/search', data={'raw_text': 't', 'images': (BytesIO(b'x'), 'q.txt')}, content_type='multipart/form-data' | search blueprint registered; uploads and mocked search service dependencies available; module Flask test client inside app context; Patched: routes.search.get_selected_fusion_endpoint; routes.search.save_search_image; Mock/setup values: ms.side_effect = ValueError('bad image') | c.post('/api/search', data={'raw_text': 't', 'images': (BytesIO(b'x'), 'q.txt')}, content_type='multipart/form-data').status_code == 400 | Passed |
| BU-SRT-11 | Get search by id error | 1. Run `tests/test_search_route.py::TestSearch::test_get_search_by_id_error` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: GET '/api/search/1' | search blueprint registered; uploads and mocked search service dependencies available; module Flask test client inside app context; Patched: routes.search.SearchService.get_search_by_id; Mock/setup values: mg.side_effect = Exception('err') | c.get('/api/search/1').status_code == 500 | Passed |
| BU-SRT-12 | Save base64 image | 1. Run `tests/test_search_route.py::TestSearch::test_save_base64_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: save_base64_image(f'data:image/png;base64,{b}'); save_base64_image(None) | search blueprint registered; uploads and mocked search service dependencies available; pytest fixture `search_app` | p; os.path.exists(p); save_base64_image(None) is None | Passed |
| BU-SSV-01 | Execute search basic | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_search_basic` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('test query') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 123; MQS.return_value = sq | r == {'search_id': 123} | Passed |
| BU-SSV-02 | Execute search late fusion | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_search_late_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('text', image='path/to/i.jpg') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 456; MQS.return_value = sq | mock_faiss.search_late_fusion.assert_called_once(); mock_faiss.search_text.assert_not_called() | Passed |
| BU-SSV-03 | Execute search fallback | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_search_fallback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('query') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: mock_faiss.search_text.return_value = {'success': False, 'products': []}; sq.search_id = 999; MQS.return_value = sq | SearchService.execute_search('query') == {'search_id': 999} | Passed |
| BU-SSV-04 | Execute rawtext search | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_rawtext_search` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_rawtext_search(original_search_id=111) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: orig.raw_text = 'original raw query'; mock_db.session.get.return_value = orig; new_sq.search_id = 888; MQS.return_value = new_sq; MRet.query.filter_by.return_value.all.return_value = [ret] | r == {'new_search_id': 888, 'original_search_id': 111}; ret.rawtext_used is True | Passed |
| BU-SSV-05 | Get search by id | 1. Run `tests/test_search_service.py::TestSearchService::test_get_search_by_id` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.get_search_by_id(1); SearchService.get_search_by_id(999) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; Mock/setup values: sq.raw_text = 'x'; sq.corrected_text = 'y'; mock_db.session.get.return_value = sq; mr.fetchall.return_value = []; mock_db.session.execute.return_value = mr; 1 additional mock/setup values | SearchService.get_search_by_id(1) is not None; SearchService.get_search_by_id(999) is None | Passed |
| BU-SSV-06 | Record click and feedback | 1. Run `tests/test_search_service.py::TestSearchService::test_record_click_and_feedback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.record_click(1, 999); SearchService.record_click(1, 2); SearchService.record_feedback(1, 2, True) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.Retrieve; Mock/setup values: MR.query.filter_by.return_value.first.return_value = ret; MR.query.filter_by.return_value.first.return_value = None | SearchService.record_click(1, 2) is True; ret.is_clicked is True; SearchService.record_feedback(1, 2, True) is True; ret.is_relevant is True; SearchService.record_click(1, 999) is False | Passed |
| BU-SSV-07 | Update client metrics | 1. Run `tests/test_search_service.py::TestSearchService::test_update_client_metrics` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.update_client_metrics(1, 150.0, 200.0) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchTime; Mock/setup values: mock_db.session.get.return_value = st; mock_db.session.get.return_value = None | SearchService.update_client_metrics(1, 150.0, 200.0) is True; SearchService.update_client_metrics(1, 150.0, 200.0) is False | Passed |
| BU-SSV-08 | Get metrics | 1. Run `tests/test_search_service.py::TestSearchService::test_get_metrics` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.get_metrics() | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: MQS.query.count.return_value = 1000; mf.count.side_effect = [500, 200, 50]; MR.query.filter.return_value = mf; MR.query.count.return_value = 2000; mock_db.session.query.return_value.scalar.side_effect = [300.5, 350.5] | r['total_searches'] == 1000; r['click_through_rate'] == 0.25 | Passed |
| BU-SSV-09 | Convert to jpg | 1. Run `tests/test_search_service.py::TestSearchService::test_convert_to_jpg` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: convert_to_jpg(png_path) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models | result.endswith('.jpg') | Passed |
| BU-SSV-10 | Execute image only search | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_image_only_search` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('', image='/t.jpg') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 801; MQS.return_value = sq; mock_faiss.search_image.return_value = {'products': [{'product_id': 7, 'score': 0.9}], 'success': True} | r == {'search_id': 801} | Passed |
| BU-SSV-11 | Execute early fusion | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_early_fusion` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('text', image='/t.png') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; services.search_service.convert_to_jpg; Mock/setup values: mock_models.return_value = {'fusion_endpoint': 'early', 'textual_model': 'm', 'visual_model': 'm', 'fused_model': 'm'}; sq.search_id = 802; MQS.return_value = sq; mock_faiss.search_early_fusion.return_value = {'products': [{'product_id': 8, 'score': 0.85}], 'success': True} | r == {'search_id': 802} | Passed |
| BU-SSV-12 | Execute iwt cross modal | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_iwt_cross_modal` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('text', search_mode='iwt') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 777; MQS.return_value = sq; mock_faiss.search_image_by_text.return_value = {'products': [{'product_id': 5, 'score': 0.9}], 'success': True} | mock_faiss.search_image_by_text.assert_called_once() | Passed |
| BU-SSV-13 | Execute twi cross modal | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_twi_cross_modal` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('text', image='/t.jpg', search_mode='twi') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 778; MQS.return_value = sq; mock_faiss.search_text_by_image.return_value = {'products': [{'product_id': 6, 'score': 0.8}], 'success': True} | mock_faiss.search_text_by_image.assert_called_once() | Passed |
| BU-SSV-14 | Execute search with image | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_search_with_image` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('text', image='uploads/q.jpg', correction_enabled=False, search_mode='std') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 321; MQS.return_value = sq | r == {'search_id': 321}; MQS.call_args.kwargs['query_image_path'] == 'uploads/q.jpg' | Passed |
| BU-SSV-15 | Timing persisted | 1. Run `tests/test_search_service.py::TestSearchService::test_timing_persisted` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('timing test', correction_enabled=True) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: sq.search_id = 555; MQS.return_value = sq | kwargs['search_id'] == 555; 'correction_time' in kwargs; 'faiss_time' in kwargs | Passed |
| BU-SSV-16 | Record feedback not found | 1. Run `tests/test_search_service.py::TestSearchService::test_record_feedback_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.record_feedback(1, 2, True) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.Retrieve; Mock/setup values: MR.query.filter_by.return_value.first.return_value = None | SearchService.record_feedback(1, 2, True) is False | Passed |
| BU-SSV-17 | Update client metrics exception | 1. Run `tests/test_search_service.py::TestSearchService::test_update_client_metrics_exception` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.update_client_metrics(1, 1, 1) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchTime; Mock/setup values: mock_db.session.get.return_value = MagicMock(); mock_db.session.commit.side_effect = Exception() | SearchService.update_client_metrics(1, 1, 1) is False | Passed |
| BU-SSV-18 | Rawtext search invalid id | 1. Run `tests/test_search_service.py::TestSearchService::test_rawtext_search_invalid_id` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_rawtext_search(original_search_id='abc') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session | raises ValueError, match='Invalid original_search_id' | Passed |
| BU-SSV-19 | Rawtext search not found | 1. Run `tests/test_search_service.py::TestSearchService::test_rawtext_search_not_found` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_rawtext_search(original_search_id=999) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; Mock/setup values: mock_db.session.get.return_value = None | raises ValueError, match='not found' | Passed |
| BU-SSV-20 | Execute search exception rollback | 1. Run `tests/test_search_service.py::TestSearchService::test_execute_search_exception_rollback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('query') | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched text_corrector_service returning corrected query metadata; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.SearchQuery; Mock/setup values: MQS.return_value = MagicMock(); mock_db.session.flush.side_effect = Exception('DB error') | raises Exception; mock_db.session.rollback.assert_called_once() | Passed |
| BU-SSV-21 | Get search by id empty | 1. Run `tests/test_search_service.py::TestSearchService::test_get_search_by_id_empty` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.get_search_by_id(1) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; Mock/setup values: sq.raw_text = 'x'; sq.corrected_text = 'y'; mock_db.session.get.return_value = sq; mr.fetchall.return_value = []; mock_db.session.execute.return_value = mr | r is not None; len(r['products']) == 0 | Passed |
| BU-SSV-22 | Get search by id with products | 1. Run `tests/test_search_service.py::TestSearchService::test_get_search_by_id_with_products` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.get_search_by_id(1) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; flask.current_app; os.path.exists; builtins.open -> unittest.mock.mock_open(read_data=b'img'); mimetypes.guess_type; Mock/setup values: sq.raw_text = 'x'; sq.corrected_text = 'y'; sq.query_image_path = None; mock_db.session.get.return_value = sq; mr.fetchall.return_value = [row]; 2 additional mock/setup values | r is not None; len(r['products']) == 1 | Passed |
| BU-SSV-23 | Db fallback search | 1. Run `tests/test_search_service.py::TestSearchService::test_db_fallback_search` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_db_fallback_search(123) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; Patched: services.search_service.SearchQuery; services.search_service.Product; flask.current_app; Mock/setup values: sq.raw_text = 'test phone'; mock_db.session.get.return_value = sq; mf.options.return_value.order_by.return_value.all.return_value = [mp1]; RealP.query.filter.return_value = mf; ca.config.get.return_value = '/tmp' | r['original_search_id'] == 123; len(r['products']) == 1 | Passed |
| BU-SSV-24 | Graceful degradation | 1. Run `tests/test_search_service.py::TestSearchService::test_graceful_degradation` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: SearchService.execute_search('raw query', correction_enabled=True) | SearchService dependencies are patched: database, text corrector, FAISS service, selected models; patched services.search_service.db session; patched FAISS service returning deterministic product/search results; patched selected-model config returning late fusion and model m; Patched: services.search_service.text_corrector_service; services.search_service.SearchQuery; services.search_service.Retrieve; services.search_service.SearchTime; Mock/setup values: mc.correct.return_value = {'corrected_text': 'raw query', 'success': False, 'changed': False, 'latency_ms': 0}; sq.search_id = 666; MQS.return_value = sq | SearchService.execute_search('raw query', correction_enabled=True)['search_id'] == 666 | Passed |
| BU-TCS-01 | Correct success | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_correct_success` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.correct('iphone') | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.requests.post; Mock/setup values: mr.json.return_value = {'corrected_query': 'iphone', 'changed': True, 'latency_ms': 10, 'model_used': 'b'}; mp.return_value = mr | r['corrected_text'] == 'iphone'; r['success'] is True | Passed |
| BU-TCS-02 | Correct errors | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_correct_errors` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.correct('test') | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.requests.post; Mock/setup values: mp.side_effect = ConnectionError(); mp.side_effect = Timeout(); mr.raise_for_status.side_effect = Exception('x'); mp.return_value = mr | r['success'] is False; r['corrected_text'] == 'test' | Passed |
| BU-TCS-03 | Available models | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_available_models` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.get_available_models() | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.requests.get; Mock/setup values: mr.status_code = 200; mr.json.return_value = {'data': {'correction_models': []}}; mp.return_value = mr | s.get_available_models()['status'] == 'success' | Passed |
| BU-TCS-04 | Correct no changed | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_correct_no_changed` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.correct('x') | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.requests.post; Mock/setup values: mr.json.return_value = {'corrected_query': 'x', 'changed': False}; mp.return_value = mr | s.correct('x')['changed'] is False | Passed |
| BU-TCS-05 | Get available models no requests | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_get_available_models_no_requests` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.get_available_models() | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.HAS_REQUESTS -> False | s.get_available_models()['status'] == 'success' | Passed |
| BU-TCS-06 | Available models fallback | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_available_models_fallback` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.get_available_models() | TextCorrectorService external correction API calls are mocked; pytest fixture `s`; Patched: services.text_corrector_service.requests.get; Mock/setup values: mp.side_effect = Exception('err') | s.get_available_models()['source'] == 'local_config' | Passed |
| BU-TCS-07 | Save engine | 1. Run `tests/test_text_corrector_service.py::TestTextCorrector::test_save_engine` with pytest
2. Arrange the listed test data, fixtures, database rows, and mocks
3. Execute the backend endpoint, service method, or config helper under test
4. Verify the Expected Output assertions | Calls: s.save_selected_engine('symspell') | TextCorrectorService external correction API calls are mocked; pytest fixture `s` | s.save_selected_engine('symspell')['status'] == 'success' | Passed |
