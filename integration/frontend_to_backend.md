| ID | Description | Steps | Test Data | Pre-condition | Expected Output | Passed/Failed |
|----|-------------|-------|-----------|---------------|-----------------|---------------|
| FB-INT-001 | Verify that submitting a text-only query from `HomePage` posts a multipart `FormData` body to `POST /api/search` with the expected fields. | 1. Render `HomePage` wrapped in `MemoryRouter` and `QueryClientProvider`. <br> 2. Type `vintage leather jacket` into the textarea (`role="search"` form). <br> 3. Click the Submit Search button. <br> 4. Capture the intercepted request body with an MSW spy handler. | Query text `vintage leather jacket`, no image, default `correctionEnabled=true`, default `searchMode="std"` | MSW handler installed for `POST /api/search` returning `{ search_id: "search-abc" }`; route stub for `/search/:searchId` to avoid navigation crash | Intercepted `FormData` contains `raw_text="vintage leather jacket"`, no `images` entry, `correction_enabled="true"`, `search_mode="std"`; handler responds HTTP 200 | Passed |
| FB-INT-002 | Verify that a successful `POST /api/search` triggers `react-router` navigation to `/search/:searchId` with `searchDuration` in `location.state`. | 1. Render `HomePage` inside a `MemoryRouter` with a `<Routes>` capturing `/search/:searchId`. <br> 2. Submit query `denim jeans`. <br> 3. Wait for `useNavigate` redirect. <br> 4. Read `useParams` and `useLocation().state` from the destination route. | Query `denim jeans`; mocked `search_id="search-xyz123"` | MSW handler returns `{ search_id: "search-xyz123" }` with ~50ms delay; `<Routes>` includes a stub destination component that renders the captured params and state | URL pathname becomes `/search/search-xyz123`; `location.state.searchDuration` is a finite positive number derived from `performance.now()` | Passed |
| FB-INT-003 | Verify that the Submit button is disabled when both query is empty and no image is attached. | 1. Render `HomePage`. <br> 2. Leave textarea empty and add no image. <br> 3. Read the `aria-label="Submit search"` button's `disabled` property. <br> 4. Attempt a click and assert no `POST /api/search` is fired. | No text, no image, default mode `std` | MSW spy handler counting `POST /api/search` invocations (initial count = 0) | Submit button has `disabled` attribute; intercepted-request counter remains 0 after the click attempt | Passed |
| FB-INT-004 | Verify that toggling the spell-correction button changes the `correction_enabled` field sent to `POST /api/search`. | 1. Render `HomePage`. <br> 2. Type `iphon 15`. <br> 3. Click the `aria-label="Spell correction, on"` button to flip it off. <br> 4. Submit the form. <br> 5. Read the captured `FormData`. | Query `iphon 15`, button toggled off | MSW spy handler capturing `FormData` of `POST /api/search` and returning `{ search_id: "s-1" }` | Captured `FormData.get("correction_enabled")` equals the string `"false"`; `aria-pressed` of the toggle is `"false"` after click | Passed |
| FB-INT-005 | Verify that selecting search mode `iwt` causes `search_mode="iwt"` to be sent in the request body. | 1. Render `HomePage`. <br> 2. Open the `SearchModeMenu` and pick `iwt`. <br> 3. Type `red sneakers`. <br> 4. Submit. <br> 5. Read captured `FormData`. | Mode `iwt`, query `red sneakers` | MSW spy handler for `POST /api/search` returning `{ search_id: "s-iwt" }` | Captured `FormData.get("search_mode")` equals `"iwt"`; `FormData.get("raw_text")` equals `"red sneakers"` | Passed |
| FB-INT-006 | Verify that switching to `twi` mode disables the textarea and requires an image before the form will submit. | 1. Render `HomePage`. <br> 2. Pick mode `twi`. <br> 3. Confirm textarea is `disabled`. <br> 4. Click Submit with no image. <br> 5. Attach a synthetic `File` and submit again. | Mode `twi`, image `File(["bytes"], "shoe.png", { type: "image/png" })` | MSW spy handler counting `POST /api/search` calls; hidden file input found by `aria-label="Add image"` | First submit attempt yields 0 intercepted calls; second submit (with image) yields exactly 1 intercepted call carrying `images` entry whose `name === "shoe.png"` | Passed |
| FB-INT-007 | Verify that an attached image file is appended to the `POST /api/search` `FormData` under the key `images`. | 1. Render `HomePage`. <br> 2. Type `summer dress`. <br> 3. Upload `dress.jpg` via the hidden file input. <br> 4. Submit. <br> 5. Inspect captured `FormData` entries. | `File(["xx"], "dress.jpg", { type: "image/jpeg" })` plus query text | MSW spy handler for `POST /api/search`; `useSearchImage` hook produces preview URL via `URL.createObjectURL` (jsdom polyfill) | Captured `FormData.get("images")` is a `File` with `name="dress.jpg"` and `type="image/jpeg"`; submit succeeds and response carries `search_id` | Passed |
| FB-INT-008 | Verify that `SearchPage` issues `GET /api/search/:searchId` on mount and renders one `ProductCard` per product in the response. | 1. Render `SearchPage` at route `/search/search-xyz`. <br> 2. Wait for the `useQuery` for `["searchResults", "search-xyz"]` to resolve. <br> 3. Count rendered `ProductCard` elements. | `searchId="search-xyz"` | MSW handler `GET /api/search/:searchId` returns the default `mockProducts` array (4 products) plus typed top-level fields | Exactly 4 product cards render; each card text contains the corresponding `brand.name` (e.g., `UrbanStyle`, `DenimCo`, `OfficePro`, `TechLife`) | Passed |
| FB-INT-009 | Verify that when `corrected_text !== raw_text`, the page shows `Showing results for: <corrected>` and `Search instead for: <raw>`. | 1. Override the `GET /api/search/:searchId` handler to return `corrected_text="leather jacket"` and `raw_text="lether jakcet"`. <br> 2. Render `SearchPage`. <br> 3. Inspect the rendered "Showing results for" block. | Override response with `corrected_text="leather jacket"`, `raw_text="lether jakcet"` | MSW `server.use(...)` to override the default handler within the test | Heading text contains `Showing results for: leather jacket`; secondary text contains a button labelled `lether jakcet` | Passed |
| FB-INT-010 | Verify that clicking the `Search instead for` button posts a new `POST /api/search` with `correction_enabled="false"` carrying the original `raw_text`. | 1. Render `SearchPage` with the override from FB-INT-009. <br> 2. Click the button labelled `lether jakcet`. <br> 3. Capture the new `POST /api/search` request `FormData`. | Captured second-call `FormData` should encode `raw_text="lether jakcet"`, `correction_enabled="false"` | Two MSW handlers: the overridden `GET /api/search/:searchId` and a spy `POST /api/search` returning `{ search_id: "search-raw" }` | Captured `FormData.get("raw_text")` equals `"lether jakcet"`; `FormData.get("correction_enabled")` equals `"false"`; navigation to `/search/search-raw` is triggered | Passed |
| FB-INT-011 | Verify that the semantic-search switch toggling off triggers `POST /api/search/db-fallback` and replaces the rendered grid with mapped `DbFallbackProduct` rows. | 1. Render `SearchPage` at `/search/search-1`. <br> 2. Click the `Switch` labelled `Semantic search` to turn it off. <br> 3. Wait for the `dbFallback` query to resolve. <br> 4. Read rendered card brand text. | Default MSW response from `POST /api/search/db-fallback` (3 products: Office Chair, Denim Jeans, Smart Speaker) | MSW handlers for both `GET /api/search/:searchId` and `POST /api/search/db-fallback` registered; `Switch` component clickable | Exactly 3 `ProductCard` elements render; brand names visible include `OfficePro`, `DenimCo`, `TechLife`; the request body sent to `/api/search/db-fallback` is JSON `{ "search_id": "search-1" }` | Passed |
| FB-INT-012 | Verify that a 500 response from `GET /api/search/:searchId` renders the error region with `role="alert"`. | 1. Override `GET /api/search/:searchId` to return HTTP 500. <br> 2. Render `SearchPage`. <br> 3. Wait for `useQuery` to settle in error state. | HTTP 500 with empty body | MSW override using `HttpResponse(null, { status: 500 })`; `QueryClient` configured with `retry: false` | An element with `role="alert"` appears containing the text `Failed to get results.` (the `fetchSearchResults` thrown message) | Passed |
| FB-INT-013 | Verify that an empty `products: []` payload renders the "No products found." status region. | 1. Override `GET /api/search/:searchId` to return a body with `products: []` and the rest of the standard schema. <br> 2. Render `SearchPage`. <br> 3. Wait for query resolution. | Override JSON `{ products: [], corrected_text: "x", raw_text: "x", search_id: "s", fusion_type: "late_fusion", ... }` | MSW override; route at `/search/s` | An element with `role="status"` is in the document containing the literal text `No products found.`; no `ProductCard` is rendered | Passed |
| FB-INT-014 | Verify that `SearchResponse` schema fields consumed by the UI (`corrected_text`, `raw_text`, `query_image`, `search_mode`, `correction_enabled`) are propagated into the `SearchBar` props on the page. | 1. Override `GET /api/search/:searchId` with explicit values: `corrected_text="bag"`, `raw_text="bgg"`, `search_mode="twi"`, `correction_enabled=false`, plus a `query_image` object. <br> 2. Render `SearchPage`. <br> 3. Inspect the `SearchBar` element. | Override response uses `search_mode: "twi"`, `correction_enabled: false` | MSW handler override; jsdom `URL.createObjectURL` polyfill for image preview | The image preview `img` element renders with `alt` derived from the override; the spell-correction button has `aria-pressed="false"`; the mode-menu button reflects `twi` | Passed |
| FB-INT-015 | Verify that clicking a product card image link navigates to `/product/:productId` and `ProductPage` issues `GET /api/products/:productId`. | 1. Render `SearchPage` so it lists `mockProducts`. <br> 2. Click the link for product id `1`. <br> 3. Wait for `ProductPage` to mount and its `useQuery` to fire. <br> 4. Capture the network request URL. | Click target product id `1` (Vintage Leather Jacket) | MSW handlers for `GET /api/search/:searchId` and `GET /api/products/:productId`; routes `/search/:id` and `/product/:id` registered | The intercepted request URL ends with `/api/products/1`; the page renders the brand `UrbanStyle` and price `$89.99` | Passed |
| FB-INT-016 | Verify that `ProductPage` renders the `Failed to fetch product.` alert when `GET /api/products/:productId` returns 404. | 1. Override `GET /api/products/:productId` to return `HttpResponse(null, { status: 404 })`. <br> 2. Render `ProductPage` at `/product/9999`. <br> 3. Wait for query failure. | Path param `productId="9999"` | MSW override; `QueryClient` with `retry: false` | An element with `role="alert"` contains the text `Product not found.` (message thrown by `fetchProductById`) | Passed |
| FB-INT-017 | Verify that clicking the like (relevant) button on a `ProductCard` posts to `POST /api/feedback` with the expected JSON body. | 1. Render `SearchPage` with `searchId="search-1"`. <br> 2. Locate a `ProductCard` for `product_id="2"`. <br> 3. Click the button with `aria-label="Product is relevant"`. <br> 4. Capture the request body. | Like vote on product id `2` within search id `search-1` | MSW spy handler for `POST /api/feedback` returning `HttpResponse.json({})` | Captured JSON body equals `{ "query_id": "search-1", "product_id": "2", "is_relevant": true }`; the like button gets `aria-pressed="true"` (optimistic update) | Passed |
| FB-INT-018 | Verify that clicking a vote optimistically updates the button before the network round-trip resolves. | 1. Render `SearchPage`. <br> 2. Configure the `POST /api/feedback` MSW handler with a 500ms delay. <br> 3. Click `aria-label="Product is not relevant"` on a card. <br> 4. Synchronously assert `aria-pressed="true"` on the dislike button before the delay elapses. | Dislike vote with 500ms artificial latency | MSW handler with `await new Promise(r => setTimeout(r, 500))` before responding | Within the same render frame, the dislike button's `aria-pressed` is `"true"`; only after `await` do the network effects complete | Passed |
| FB-INT-019 | Verify that a failed `POST /api/feedback` (HTTP 500) reverts the optimistic vote state. | 1. Override `POST /api/feedback` to return HTTP 500. <br> 2. Render `SearchPage`. <br> 3. Stub `window.alert` with `vi.fn()`. <br> 4. Click the like button on a product card. <br> 5. Wait for mutation `onError`. | Like click against a 500-returning feedback endpoint | MSW override; `vi.spyOn(window, "alert")` | After the error settles, the like button's `aria-pressed` is back to `"false"` and `window.alert` was called once with `"Failed to submit vote"` | Passed |
| FB-INT-020 | Verify that clicking the same vote twice clears the vote and does not fire a second `POST /api/feedback`. | 1. Render `SearchPage`. <br> 2. Click the like button on a product card. <br> 3. Wait for the success response. <br> 4. Click the same like button again. <br> 5. Inspect the request counter. | Same product, two clicks on like | MSW spy handler counting calls to `POST /api/feedback` | Counter equals exactly 1; after the second click the like button's `aria-pressed` is `"false"` (vote cleared per `ProductCard` toggle logic) | Passed |
| FB-INT-021 | Verify that the score badge text is computed from `Product.score` rounded to a percentage. | 1. Override `GET /api/search/:searchId` to set the first product's `score=0.926`. <br> 2. Render `SearchPage`. <br> 3. Read the badge text on the first card. | Override product list with `score: 0.926` | MSW override; the `ProductCard` rendering branch for `score !== undefined` | The badge contains the text `Score: 93%` (per `Math.round(score * 100)` in `ProductCard.tsx`) | Passed |
| FB-INT-022 | Verify that products without a `score` field do not render the score badge. | 1. Override `GET /api/search/:searchId` to omit `score` on all products. <br> 2. Render `SearchPage`. <br> 3. Query for any element containing the text `Score:`. | Override products with `score` undefined | MSW override; `mockProducts` without `score`, `text_score`, `image_score`, `combined_score` | `screen.queryByText(/Score:/)` returns `null`; product cards still render with brand and price | Passed |
| FB-INT-023 | Verify that after a successful search submission `POST /api/analytics/search-duration` is called with `search_id`, `search_duration`, `product_load_duration`. | 1. Render `HomePage` inside a `<Routes>` that mounts `SearchPage` at `/search/:searchId`. <br> 2. Submit a query. <br> 3. Wait for `SearchPage` to load results. <br> 4. Capture the JSON body of the analytics call. | Default search payload, `searchId="search-abc"`; `sessionStorage` cleared in `beforeEach` | MSW handlers for `POST /api/search`, `GET /api/search/:searchId`, and a spy for `POST /api/analytics/search-duration`; `sessionStorage.clear()` in setup | Captured JSON contains keys `search_id` (string), `search_duration` (number > 0), `product_load_duration` (number >= 0); the analytics call fires exactly once | Passed |
| FB-INT-024 | Verify that the analytics call is suppressed when the same `searchId` was already recorded in `sessionStorage`. | 1. Pre-populate `sessionStorage` with `recorded_search_search-abc` = `"true"`. <br> 2. Render `SearchPage` at `/search/search-abc` with `location.state.searchDuration=1234`. <br> 3. Wait for results. <br> 4. Inspect analytics call counter. | `searchId="search-abc"`, `sessionStorage` already contains the marker key | MSW spy handler counting `POST /api/analytics/search-duration` calls | Counter remains 0; product grid still renders normally | Passed |
| FB-INT-025 | Verify that a network failure on `POST /api/search` shows an inline error inside `SearchBar` and does not navigate. | 1. Override `POST /api/search` to return `HttpResponse(null, { status: 500 })`. <br> 2. Render `HomePage` inside a `<Routes>`. <br> 3. Submit `phone case`. <br> 4. Wait for the mutation to settle in error state. | HTTP 500 from `POST /api/search` | MSW override; `<Routes>` so navigation can be observed | A `<p id="search-error" role="alert">` appears with text `Failed to submit search request`; URL pathname remains `/`; the SearchBar form ring becomes red | Passed |
| FB-INT-026 | Verify that submitting more than 300 characters disables the submit button (character limit enforcement). | 1. Render `HomePage`. <br> 2. Paste a 301-character string into the textarea. <br> 3. Inspect the submit button's `disabled` property. <br> 4. Read the character-count text. | A string of length 301 (`"a".repeat(301)`) | No MSW interaction needed; `charLimit = 300` constant in `SearchBar.tsx` | Submit button has `disabled` attribute; the count text reads `301/300` and renders with the `text-red-500` class on the count span | Passed |
| FB-INT-027 | Verify that the typed `SearchResponse` fields used to drive the UI never trigger a runtime crash when optional `score`, `text_score`, `image_score`, `rank`, `query_image` are absent. | 1. Override `GET /api/search/:searchId` to return one product with only the required fields and no `query_image`. <br> 2. Render `SearchPage`. <br> 3. Confirm no `console.error` is emitted by React. | Minimal `Product` `{ product_id, name, brand, price, description, is_relevant, images, categories, subcategory }` | MSW override; `vi.spyOn(console, "error")` to detect React warnings/errors | The page renders one card; `console.error` was not called; `screen.queryByText(/Score:/)` returns `null`; no image preview is rendered in `SearchBar` | Passed |
| FB-INT-028 | Verify that the request `Content-Type` for `POST /api/search/db-fallback` is `application/json` and the body is the search id. | 1. Render `SearchPage` at `/search/search-2`. <br> 2. Toggle the semantic-search `Switch` off. <br> 3. Capture the request `Content-Type` header and the parsed JSON body. | Toggle off semantic search on `searchId="search-2"` | MSW spy handler that reads `request.headers.get("content-type")` and `await request.json()` for `POST /api/search/db-fallback` | Captured header equals `application/json`; captured JSON body equals `{ "search_id": "search-2" }`; response renders 3 product cards | Passed |
