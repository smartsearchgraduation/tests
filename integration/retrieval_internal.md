| ID | Description | Steps | Test Data | Pre-condition | Expected Output | Passed/Failed |
|----|-------------|-------|-----------|---------------|-----------------|---------------|
| RI-L2-01 | Add product then text-search round-trip | 1. POST /api/retrieval/add-product with id="rt1", name="Red Sneaker", textual_model_name="ViT-B/32", visual_model_name="ViT-B/32"<br>2. POST /api/retrieval/search/text with text="Red Sneaker", textual_model_name="ViT-B/32", top_k=5 | full add + search bodies | clean caches, stub_managers, tmp_index_dir as DATA_BASE_PATH | Add returns 201; search returns 200 with non-empty results; top result product_id=="rt1" | Passed |
| RI-L2-02 | Add with image then image-search round-trip | 1. POST add-product id="rt2" with images=[tmp_image_path] and ViT-B/32 models<br>2. POST /search/image with image=tmp_image_path | CLIP body with image | tmp_image_path exists, stub_managers | Add 201 with visual_vector_ids length 1; search 200 with top result product_id=="rt2" | Passed |
| RI-L2-03 | Delete after add removes from search | 1. Add id="rt3"<br>2. DELETE /api/retrieval/delete-product/rt3<br>3. POST /search/text with same text | full bodies | rt3 added | DELETE 200 with total_removed > 0; subsequent search 200 with no result for product_id=="rt3" | Passed |
| RI-L2-04 | top_k clamp end-to-end on /search/text | 1. Add 3 products<br>2. POST /search/text with top_k=200 (above MAX_TOP_K) | top_k=200 | 3 products indexed | 200; len(results) <= 100 (MAX_TOP_K); meta.total_results <= 3 | Passed |
| RI-L2-05 | image-by-text cross-modal round-trip | 1. Add product "rt5" with fused_model_name="ViT-B/32" and 1 image<br>2. POST /search/image-by-text with text="Red Sneaker", fused_model_name="ViT-B/32" | full body | fused index built, stub_managers | 200; results contain product_id=="rt5" | Passed |
| RI-L2-06 | text-by-image cross-modal round-trip | 1. Add product "rt6" with fused index and image=tmp_image_path<br>2. POST /search/text-by-image with image=tmp_image_path, fused_model_name="ViT-B/32" | CLIP body | fused index has rt6 | 200; results contain entry with product_id=="rt6" | Passed |
| RI-L2-07 | index-stats reflects added products | 1. Add 2 products with ViT-B/32 (1 with image, 1 text-only)<br>2. GET /api/retrieval/index-stats | n/a | clean caches, then 2 adds | 200; indices contain a key whose value has textual >= 2 and visual >= 1 | Passed |
| RI-L2-08 | Adding the same product twice deduplicates at search time | 1. Add id="rt8" with one image<br>2. Add id="rt8" again with same payload<br>3. POST /search/text | duplicate adds | n/a | 2nd add 200 with skipped==True; search 200 with rt8 appearing exactly once | Passed |
| RI-L2-09 | Persistence smoke test (save then reload then search) | 1. Add id="rt9" (causes save to tmp_index_dir)<br>2. Clear _faiss_managers cache only to force reload from disk<br>3. POST /search/text | n/a | tmp_index_dir persists, lazy-load triggers from disk | 200; search results include rt9 | Passed |
| RI-L2-10 | Update endpoint replaces embeddings | 1. Add id="rt10" with name="Old"<br>2. PUT /api/retrieval/update-product/rt10 with name="Brand New Hat" and same models<br>3. POST /search/text with text="Brand New Hat" | full bodies | rt10 added | PUT 200 with non-zero removed_counts; search 200 with top result product_id=="rt10" | Passed |
| RI-L2-11 | Late fusion end-to-end | 1. Add id="rt11" with 1 image and ViT-B/32<br>2. POST /search/late with text + text_weight=0.5 + image + models | full late body | rt11 in textual and visual indices | 200; results contain product_id=="rt11"; meta.text_weight==0.5 and meta.image_weight==0.5 | Passed |
| RI-L2-12 | Cross-modal rejected for non-multimodal model end-to-end | 1. Add a product using textual_model_name="bge-large-en-v1.5"<br>2. POST /search/image-by-text with fused_model_name="bge-large-en-v1.5" | non-multimodal | bge entry in MODEL_REGISTRY (type "textual") | 400; message contains "multimodal" | Passed |
| RI-L2-13 | /search/late combined_score formula end-to-end | 1. Add id="rl1" and id="rl2" via /api/retrieval/add-product with ViT-B/32 textual+visual and one image each<br>2. POST /search/late with text="query", text_weight=0.4, image=tmp_image_path, ViT-B/32 models<br>3. From the top result assert combined_score == round(0.4 * text_score + 0.6 * image_score, 6) | text_weight=0.4; two indexed products | clean caches; stub_managers; tmp_index_dir | 200; for top result combined_score matches the formula within 2e-6 | Passed |
