| ID | Description | Steps | Test Data | Pre-condition | Expected Output | Passed/Failed |
|----|-------------|-------|-----------|---------------|-----------------|---------------|
| BC-INT-001 | Verify that POST `/api/search` invokes the Correction service and persists `corrected_text` on the `SearchQuery` row. | 1. Patch `services.text_corrector_service.requests.post` to return `{'corrected_query':'iphone','changed':True,'latency_ms':12,'model_used':'byt5-small'}`. <br> 2. POST form `raw_text=iphnoe` to `/api/search`. <br> 3. Query `SearchQuery` by returned `search_id`. | Form: `raw_text=iphnoe`; Mock JSON: `{"corrected_query":"iphone","changed":true,"latency_ms":12,"model_used":"byt5-small"}` | App fixture from `Backend/tests/conftest.py`; FAISS service mocked to return one product. | HTTP 201 with body `{"search_id": <int>}`; DB row has `raw_text='iphnoe'` and `corrected_text='iphone'`. | Passed |
| BC-INT-002 | Verify that the Backend forwards `engine` form field as the `model` key in the Correction `/correct` payload. | 1. Patch `requests.post` and capture call args. <br> 2. POST `/api/search` with `raw_text=laptp&engine=symspell`. <br> 3. Inspect captured request payload. | Form: `raw_text=laptp, engine=symspell` | Correction `/correct` mocked; FAISS mocked to return empty results. | The captured POST body to Correction equals `{"query":"laptp","model":"symspell_keyboard"}` (mapped via engine_map). | Passed |
| BC-INT-003 | Verify that when `correction_enabled=false`, the Backend does NOT call the Correction service and writes `corrected_text == raw_text`. | 1. Patch `requests.post` with `assert_not_called` afterwards. <br> 2. POST `/api/search` with `raw_text=samsng&correction_enabled=false`. <br> 3. Fetch `SearchQuery`. | Form: `raw_text=samsng, correction_enabled=false` | App fixture; FAISS mocked to return empty. | HTTP 201; `requests.post` to Correction was not called; DB `corrected_text == 'samsng'`. | Passed |
| BC-INT-004 | Verify that a Correction `ConnectionError` triggers fallback: search proceeds with original text, `success=false`, no 5xx returned. | 1. Patch `requests.post` to raise `requests.exceptions.ConnectionError`. <br> 2. POST `/api/search` with `raw_text=phne`. <br> 3. Fetch `SearchQuery`. | Form: `raw_text=phne`; Mock: `ConnectionError` | App fixture; FAISS mocked to return empty. | HTTP 201 with `search_id`; `corrected_text == 'phne'`; `Retrieve.correction_engine` reflects fallback (`'UNKNOWN'` or engine arg). | Passed |
| BC-INT-005 | Verify that Correction request timeout (`requests.exceptions.Timeout`) degrades gracefully without raising 500. | 1. Patch `requests.post` to raise `Timeout`. <br> 2. POST `/api/search` with `raw_text=tablt`. | Form: `raw_text=tablt`; Mock: `Timeout` | App fixture; FAISS mocked. | HTTP 201; `corrected_text == 'tablt'`; `SearchTime.correction_time` is a non-null float. | Passed |
| BC-INT-006 | Verify that Correction returning HTTP 500 is treated as a failure and original text is preserved. | 1. Patch `requests.post` so `raise_for_status()` raises `HTTPError`. <br> 2. POST `/api/search` with `raw_text=mous`. | Form: `raw_text=mous`; Mock: HTTPError on raise_for_status | App fixture; FAISS mocked. | HTTP 201; persisted `corrected_text == 'mous'`; logs contain `[TextCorrector] Error`. | Passed |
| BC-INT-007 | Verify that Correction returning malformed JSON missing `corrected_query` falls back to legacy key `corrected`, then to `raw_text`. | 1. Patch `requests.post` to return `{"corrected":"phone","changed":true}` then `{}`. <br> 2. POST `/api/search` twice, capturing each `corrected_text`. | Mocks: `{"corrected":"phone","changed":true}` and `{}` | App fixture; FAISS mocked. | First search persists `corrected_text='phone'`; second persists `corrected_text == raw_text`. | Passed |
| BC-INT-008 | Verify that when the Correction response includes `latency_ms`, it is stored in the `search_time.correction_time` column. | 1. Patch `requests.post` to return `latency_ms=42`. <br> 2. POST `/api/search`. <br> 3. Read `SearchTime` for the new `search_id`. | Mock: `{"corrected_query":"x","changed":false,"latency_ms":42,"model_used":"byt5-small"}` | App fixture; FAISS mocked. | `SearchTime.correction_time` is a positive float (wall-clock duration recorded by service); `SearchTime.search_id == search_id`. | Passed |
| BC-INT-009 | Verify that `model_used` returned by Correction is propagated to `Retrieve.correction_engine`. | 1. Patch `requests.post` to return `model_used=byt5-large`. <br> 2. POST `/api/search`. <br> 3. Query `Retrieve` rows for that `search_id`. | Mock: `{"corrected_query":"y","changed":true,"latency_ms":5,"model_used":"byt5-large"}` | App fixture; FAISS mocked to return at least 1 product `{"product_id":1,"score":0.95}`. | At least one `Retrieve` row exists with `correction_engine == 'byt5-large'`. | Passed |
| BC-INT-010 | Verify GET `/api/correction/models` returns 200 with engines list when `text_corrector_service.get_available_models` succeeds. | 1. Patch `routes.correction.text_corrector_service.get_available_models` to return success payload. <br> 2. GET `/api/correction/models`. | Mock return: `{"status":"success","data":{"engines":[{"name":"byt5-small","description":"ByT5"}]},"source":"correction_service"}` | App fixture. | HTTP 200; JSON body has `status=='success'` and `data.engines` is a non-empty list. | Passed |
| BC-INT-011 | Verify GET `/api/correction/models` falls back to `local_config` when the upstream Correction `/models` endpoint is unreachable. | 1. Patch `services.text_corrector_service.requests.get` to raise `ConnectionError`. <br> 2. GET `/api/correction/models`. | Mock: `ConnectionError` on `/models` | App fixture. | HTTP 200; `source == 'local_config'`; `data.engines` contains `symspell_keyboard` and `byt5-small`. | Passed |
| BC-INT-012 | Verify GET `/api/correction/models` returns HTTP 500 when service-layer returns `status=='error'`. | 1. Patch `text_corrector_service.get_available_models` to return `{'status':'error','error':'boom'}`. <br> 2. GET `/api/correction/models`. | Mock return: `{"status":"error","error":"boom"}` | App fixture. | HTTP 500; body has `status=='error'` and `error=='boom'`. | Passed |
| BC-INT-013 | Verify POST `/api/correction/selected-engine/save` with valid `engine` returns 200 and persists the selection via `save_selected_engine`. | 1. Patch `text_corrector_service.save_selected_engine` to return success. <br> 2. POST JSON `{"engine":"symspell_keyboard"}`. | Body: `{"engine":"symspell_keyboard"}` | App fixture. | HTTP 200; body `data.engine == 'symspell_keyboard'`; mock called once with `'symspell_keyboard'`. | Passed |
| BC-INT-014 | Verify POST `/api/correction/selected-engine/save` with empty body returns HTTP 400 with `Missing 'engine' field`. | 1. POST `/api/correction/selected-engine/save` with body `{}`. | Body: `{}` | App fixture. | HTTP 400; JSON `{"status":"error","error":"Missing 'engine' field"}`. | Passed |
| BC-INT-015 | Verify POST `/api/search` with `correction_enabled=true` (default) and a no-op correction (`changed=false`) still returns 201 and `corrected_text == raw_text`. | 1. Patch `requests.post` to return `{"corrected_query":"phone","changed":false,"latency_ms":3,"model_used":"byt5-small"}`. <br> 2. POST `/api/search` with `raw_text=phone`. | Form: `raw_text=phone`; Mock: changed=false | App fixture; FAISS mocked. | HTTP 201; DB `corrected_text == 'phone'`; `Retrieve.correction_engine == 'byt5-small'`. | Passed |
| BC-INT-016 | Verify POST `/api/search` with neither `raw_text` nor `image` returns 400 BEFORE invoking Correction. | 1. Patch `requests.post`. <br> 2. POST `/api/search` with empty form. | Form: `{}` | App fixture. | HTTP 400 with `error=="Either 'raw_text' or 'image' must be provided"`; `requests.post` to Correction not called. | Passed |
| BC-INT-017 | Verify POST `/api/search` with `search_mode=invalid` returns 400 BEFORE invoking Correction. | 1. Patch `requests.post`. <br> 2. POST `/api/search` with `raw_text=x&search_mode=foo`. | Form: `raw_text=x, search_mode=foo` | App fixture. | HTTP 400 with `error` mentioning `Invalid search_mode`; Correction not called. | Passed |
| BC-INT-018 | Verify that an unknown engine value is forwarded as-is to Correction (engine_map fallthrough). | 1. Patch `requests.post` and capture payload. <br> 2. POST `/api/search` with `raw_text=hat&engine=qwen-3.5-2b`. | Form: `raw_text=hat, engine=qwen-3.5-2b` | App fixture; FAISS mocked. | Captured Correction payload `model == 'qwen-3.5-2b'` (mapped passthrough); HTTP 201. | Passed |
| BC-INT-019 | Verify that an oversized `raw_text` (10,000 chars) is forwarded to Correction without truncation by the Backend. | 1. Patch `requests.post` and capture body. <br> 2. POST `/api/search` with `raw_text` of 10000 'a's. | Form: `raw_text='a'*10000` | App fixture; FAISS mocked. | Captured `query` length == 10000; HTTP 201. | Passed |
| BC-INT-020 | Verify that Backend uses 10s timeout when calling Correction `/correct`. | 1. Patch `requests.post` and inspect kwargs. <br> 2. POST `/api/search` with `raw_text=cat`. | Form: `raw_text=cat` | App fixture; FAISS mocked. | `requests.post` called with `timeout=10`. | Passed |
| BC-INT-021 | Verify that GET `/api/search/<id>` returns the previously-persisted `corrected_text` from the Correction round-trip. | 1. Patch `requests.post` to return `corrected_query='shoes'`. <br> 2. POST `/api/search` with `raw_text=shoez`. <br> 3. GET `/api/search/<returned_id>`. | Mock: `{"corrected_query":"shoes","changed":true,"latency_ms":7,"model_used":"byt5-small"}` | App fixture; FAISS mocked to return at least one product. | HTTP 200; response JSON has `raw_text=='shoez'` and `corrected_text=='shoes'`. | Passed |
| BC-INT-022 | Verify that `search_mode='iwt'` propagates `corrected_text` (not `raw_text`) to the FAISS image-by-text call. | 1. Patch `requests.post` to return `corrected_query='red shoes'`. <br> 2. Patch `faiss_service.search_image_by_text` and capture args. <br> 3. POST `/api/search` with `raw_text=red shoez&search_mode=iwt`. | Form: `raw_text=red shoez, search_mode=iwt` | App fixture; FAISS mock. | `search_image_by_text` called with `text='red shoes'`; HTTP 201. | Passed |
| BC-INT-023 | Verify schema contract: Backend tolerates Correction response containing extra unknown keys without raising. | 1. Patch `requests.post` to return `{"corrected_query":"phone","changed":true,"latency_ms":5,"model_used":"byt5-small","experimental_field":"x","debug":{"a":1}}`. <br> 2. POST `/api/search`. | Mock: response with extra keys | App fixture; FAISS mocked. | HTTP 201; `corrected_text=='phone'` persisted; no exception in logs. | Passed |
| BC-INT-024 | Verify that when Correction returns `success=false` (post-fallback shape), Backend still records a `Retrieve.correction_engine` value and does NOT abort the request. | 1. Patch `requests.post` to raise `ConnectionError` (forces fallback path returning `success=False`). <br> 2. POST `/api/search` with `raw_text=ipd`. | Form: `raw_text=ipd`; Mock: ConnectionError | App fixture; FAISS mocked to return one product. | HTTP 201; `Retrieve.correction_engine` is non-null (defaults to engine arg or `'UNKNOWN'`); `correction_enabled` column on `SearchQuery` is `True`. | Passed |
| BC-INT-025 | Verify environment variable `CORRECTION_SERVICE_URL` is honored by the Backend's outbound POST URL. | 1. Set `CORRECTION_SERVICE_URL=http://mock-correction:9999/correct`. <br> 2. Re-instantiate `TextCorrectorService()`. <br> 3. Patch `requests.post` and capture URL. <br> 4. Call `.correct('q')`. | Env: `CORRECTION_SERVICE_URL=http://mock-correction:9999/correct` | App fixture. | `requests.post` first positional arg == `'http://mock-correction:9999/correct'`. | Passed |
| BC-INT-026 | Verify that `SearchTime` row is created with all four timing columns populated after a successful Correction round-trip. | 1. Patch `requests.post` to return `latency_ms=15`. <br> 2. POST `/api/search`. <br> 3. SELECT `SearchTime` by `search_id`. | Mock: standard success | App fixture; FAISS mocked. | Row exists with `correction_time>=0`, `faiss_time>=0`, `db_time>=0`, `backend_total_time>=0`, all non-null. | Passed |
| BC-INT-027 | Verify POST `/api/correction/selected-engine/save` with `engine=""` (empty string) returns 400. | 1. POST `/api/correction/selected-engine/save` with `{"engine":""}`. | Body: `{"engine":""}` | App fixture. | HTTP 400; body `error == "Missing 'engine' field"`. | Passed |
| BC-INT-028 | Verify that two consecutive `/api/search` calls with different engines result in two `Retrieve` rows whose `correction_engine` differ accordingly. | 1. Patch `requests.post`: first call returns `model_used=symspell_keyboard`, second returns `model_used=byt5-small`. <br> 2. POST `/api/search` with `engine=symspell`, then with `engine=byt5`. <br> 3. Query both `Retrieve` rows. | Two mocks with different `model_used` values | App fixture; FAISS mocked to return one product per call. | First search's `Retrieve.correction_engine == 'symspell_keyboard'`; second's == `'byt5-small'`; both `search_id`s are distinct. | Passed |
