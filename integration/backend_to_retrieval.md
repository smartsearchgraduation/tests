| ID | Description | Steps | Test Data | Pre-condition | Expected Output | Passed/Failed |
|----|-------------|-------|-----------|---------------|-----------------|---------------|
| BR-INT-001 | Verify that `POST /api/retrieval/search/text` forwards a valid text query to FAISS and returns its products payload. | 1. Patch `requests.post` to return 200 with `{"status":"success","products":[{"product_id":"p1","score":0.91}]}`. <br> 2. POST to `/api/retrieval/search/text` with body `{"text":"running shoes","top_k":5}`. <br> 3. Assert HTTP 200 and JSON shape. | Request body `{"text":"running shoes","top_k":5}`; mocked FAISS response with one product. | Flask app + `retrieval_bp` registered (from `Backend/tests/conftest.py`); mocked FAISS HTTP layer. | HTTP 200; response JSON has `status: "success"` and `products` is a non-empty list whose entries include `product_id` and `score`. | Passed |
| BR-INT-002 | Verify that `POST /api/retrieval/search/text` returns 400 when the request body is empty. | 1. POST to `/api/retrieval/search/text` with no JSON body. <br> 2. Assert HTTP 400 and `error` key. | Empty/missing JSON body. | Flask app with `retrieval_bp`. | HTTP 400; JSON `{"status":"error","error":"Request body is required"}`. | Passed |
| BR-INT-003 | Verify that `POST /api/retrieval/search/text` returns 400 when the `text` field is missing. | 1. POST to `/api/retrieval/search/text` with body `{"top_k":5}`. <br> 2. Assert HTTP 400. | `{"top_k":5}` (no `text`). | Flask app with `retrieval_bp`. | HTTP 400; JSON error message contains `Missing required field: text`. | Passed |
| BR-INT-004 | Verify that `POST /api/retrieval/search/late` requires both `text` and `image` and returns 400 when image only is given. | 1. POST to `/api/retrieval/search/late` with `{"image":"/tmp/q.jpg"}`. <br> 2. Assert HTTP 400 and the rejection message. | `{"image":"/tmp/q.jpg"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error `Text is required for late fusion search`. | Passed |
| BR-INT-005 | Verify that `POST /api/retrieval/search/late` happy path forwards both modalities and returns the FAISS result. | 1. Create a temp image file via `tmp_path`. <br> 2. Patch `requests.post` to return 200 with `{"status":"success","products":[{"product_id":"p1","text_score":0.7,"image_score":0.6,"combined_score":0.65}]}`. <br> 3. POST `{"text":"red shoe","image":"<tmp_path>","text_weight":0.7}`. | Body containing `text`, `image`, `text_weight`; mocked FAISS late-fusion response with `text_score`, `image_score`, `combined_score`. | `tmp_path` fixture; Flask app with `retrieval_bp`. | HTTP 200; products[0] preserves `text_score`, `image_score`, `combined_score`. | Passed |
| BR-INT-006 | Verify that `POST /api/retrieval/search/early` returns 400 when only image is provided. | 1. POST to `/api/retrieval/search/early` with `{"image":"/tmp/q.jpg"}`. <br> 2. Assert HTTP 400. | `{"image":"/tmp/q.jpg"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error contains `Missing required field: text`. | Passed |
| BR-INT-007 | Verify that `POST /api/retrieval/search/image` returns 400 when `image` field is missing. | 1. POST to `/api/retrieval/search/image` with `{"top_k":5}`. <br> 2. Assert HTTP 400. | `{"top_k":5}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error contains `Missing required field: image`. | Passed |
| BR-INT-008 | Verify that `POST /api/retrieval/search/image-by-text` rejects empty whitespace text with 400. | 1. POST to `/api/retrieval/search/image-by-text` with `{"text":"   "}`. <br> 2. Assert HTTP 400. | `{"text":"   "}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error contains `Missing required field: text`. | Passed |
| BR-INT-009 | Verify that `POST /api/retrieval/search/text-by-image` forwards image path to FAISS and returns 200 with results. | 1. Create a temp image via `tmp_path`. <br> 2. Patch `requests.post` to return 200 with `{"status":"success","results":[{"product_id":"p2","score":0.42}]}`. <br> 3. POST `{"image":"<tmp_path>","fused_model_name":"ViT-B/32","top_k":3}`. | Body with `image`, `fused_model_name`, `top_k`; mocked FAISS response uses `results` key. | `tmp_path` fixture; Flask app with `retrieval_bp`. | HTTP 200; response has either `products` or `results` populated and a `status` of `success`. | Passed |
| BR-INT-010 | Verify that `POST /api/retrieval/add-product` happy path returns 200 with `textual_vector_id` and `visual_vector_ids`. | 1. Patch `requests.post` to return 200 with `{"status":"success","details":{"textual_vector_id":42,"visual_vector_ids":[1,2,3]}}`. <br> 2. POST minimal valid body `{"id":"sku-001","name":"Cotton T-Shirt","description":"Soft cotton tee","brand":"Acme","category":"Tops","price":19.99,"images":[]}`. | Body matches request shape parsed by `add_product` route; mocked FAISS returns vector IDs. | Flask app with `retrieval_bp`; `UPLOAD_FOLDER` set in app config. | HTTP 200; `details.textual_vector_id == 42`; `details.visual_vector_ids == [1,2,3]`; `details.images_processed == 3`. | Passed |
| BR-INT-011 | Verify that `POST /api/retrieval/add-product` returns 400 when `id` is missing. | 1. POST `/api/retrieval/add-product` with body `{"name":"X"}`. <br> 2. Assert HTTP 400. | `{"name":"X"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error `Missing required field: id`. | Passed |
| BR-INT-012 | Verify that `POST /api/retrieval/add-product` returns 400 when `name` is missing. | 1. POST `/api/retrieval/add-product` with `{"id":"sku-001"}`. <br> 2. Assert HTTP 400. | `{"id":"sku-001"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error `Missing required field: name`. | Passed |
| BR-INT-013 | Verify that `POST /api/retrieval/add-product` returns 400 when `price` is not coercible to float. | 1. POST `/api/retrieval/add-product` with `{"id":"sku-1","name":"X","price":"not-a-number"}`. <br> 2. Assert HTTP 400. | `{"id":"sku-1","name":"X","price":"not-a-number"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error `Invalid price value`. | Passed |
| BR-INT-014 | Verify that `POST /api/retrieval/add-product` returns the `skipped` shape when FAISS reports the product already has embeddings for the active model. | 1. Patch `requests.post` to return 200 with `{"status":"success","details":{"skipped":true}}`. <br> 2. POST a valid body. <br> 3. Assert response. | Mocked FAISS returns success with `details.skipped: true`. | Flask app with `retrieval_bp`. | HTTP 200; JSON `status: "skipped"` and `message` mentions "already has embeddings". | Passed |
| BR-INT-015 | Verify that `POST /api/retrieval/add-product` propagates HTTP 500 when FAISS returns success but no `textual_vector_id`. | 1. Patch `requests.post` to return 200 with `{"status":"success","details":{"visual_vector_ids":[1]}}`. <br> 2. POST a valid body. | Mocked FAISS response missing `textual_vector_id`. | Flask app with `retrieval_bp`. | HTTP 500; JSON `status: "error"` with message about missing textual vector ID. | Passed |
| BR-INT-016 | Verify that `PUT /api/retrieval/update-product/<id>` builds the FAISS URL with the path parameter and returns 200 on success. | 1. Patch `requests.put` to return 200 with `{"status":"success","details":{"updated":true}}`. <br> 2. PUT `/api/retrieval/update-product/sku-9` with body `{"name":"X","price":1.0}`. <br> 3. Assert URL passed to mock contains `/update-product/sku-9`. | Path param `sku-9`; minimal body. | Flask app with `retrieval_bp`. | HTTP 200; `details.updated == true`; mocked URL includes `sku-9`. | Passed |
| BR-INT-017 | Verify that `DELETE /api/retrieval/delete-product/<id>` translates FAISS 404 into a 200 success with `not in FAISS index` message. | 1. Patch `requests.delete` to return 404. <br> 2. DELETE `/api/retrieval/delete-product/missing-sku`. | Path param `missing-sku`; mocked 404. | Flask app with `retrieval_bp`. | HTTP 200; JSON `status: "success"`, `message` contains `not in FAISS index`. | Passed |
| BR-INT-018 | Verify that `DELETE /api/retrieval/delete-product/<id>` propagates a FAISS 500 as an error response. | 1. Patch `requests.delete` to return 500 with `{"error":"boom"}`. <br> 2. DELETE `/api/retrieval/delete-product/sku-9`. | Mocked 500 response. | Flask app with `retrieval_bp`. | HTTP 500; JSON `status: "error"` and error message references `FAISS Error (500)`. | Passed |
| BR-INT-019 | Verify that `GET /api/retrieval/index-stats` returns the FAISS-provided `data` and `indices` payload. | 1. Patch `requests.get` to return 200 with `{"status":"success","data":{"models":{"ViT-B/32":{"textual":10}}}}`. <br> 2. GET `/api/retrieval/index-stats`. | Mocked FAISS index-stats response. | Flask app with `retrieval_bp`. | HTTP 200; JSON has `status: "success"`, `data` and `indices` keys present. | Passed |
| BR-INT-020 | Verify that `GET /api/retrieval/models` falls back to local config when the FAISS service is offline. | 1. Patch `requests.get` to raise `requests.exceptions.ConnectionError`. <br> 2. GET `/api/retrieval/models`. | Connection error raised by mocked transport. | Flask app with `retrieval_bp`; `config/models.py` importable. | HTTP 200; JSON has `status: "success"`, `source: "local_config"`, `data.textual_models` non-empty. | Passed |
| BR-INT-021 | Verify that `POST /api/retrieval/selected-models` rejects an unknown textual model name. | 1. Patch `faiss_service.get_available_model_ids` to return `["ViT-B/32","ViT-L/14"]`. <br> 2. POST `/api/retrieval/selected-models` with `{"textual_model":"BOGUS","visual_model":"ViT-B/32"}`. | Unknown textual model in body. | Flask app with `retrieval_bp`. | HTTP 400; JSON error contains `Invalid textual model: BOGUS`. | Passed |
| BR-INT-022 | Verify that `POST /api/retrieval/fusion-endpoint` rejects a value outside `{late, early}`. | 1. POST `/api/retrieval/fusion-endpoint` with `{"fusion_endpoint":"middle"}`. <br> 2. Assert HTTP 400. | `{"fusion_endpoint":"middle"}`. | Flask app with `retrieval_bp`. | HTTP 400; JSON error contains `Invalid fusion_endpoint: middle`. | Passed |
| BR-INT-023 | Verify that `DELETE /api/retrieval/clear-index` proxies a 200 success response with `deleted_count`. | 1. Patch `requests.delete` to return 200 with `{"deleted_count":12}`. <br> 2. DELETE `/api/retrieval/clear-index`. | Mocked FAISS clear-index 200. | Flask app with `retrieval_bp`. | HTTP 200; JSON `status: "success"`, `details.deleted_count == 12`. | Passed |
| BR-INT-024 | Verify that `DELETE /api/retrieval/clear-index` returns success with note when FAISS responds 404. | 1. Patch `requests.delete` to return 404. <br> 2. DELETE `/api/retrieval/clear-index`. | Mocked FAISS 404 (endpoint not found). | Flask app with `retrieval_bp`. | HTTP 200; JSON `status: "success"`, `message` includes "endpoint not available", `details.deleted_count == 0`. | Passed |
| BR-INT-025 | Verify that the `POST /api/search` flow (text only) calls `faiss_service.search_text` and persists a `SearchQuery` row whose ID is returned. | 1. Patch `text_corrector_service.correct` to return `{"corrected_text":"laptop","engine":"symspell","latency_ms":1.0}`. <br> 2. Patch `faiss_service.search_text` to return `{"status":"success","products":[{"product_id":1,"score":0.8}]}`. <br> 3. POST `/api/search` form `raw_text=laptop&search_mode=std&correction_enabled=true`. | Form-encoded body; mocks for correction and FAISS. | Flask app with `search_bp` + in-memory SQLite via `db_session` fixture. | HTTP 201; JSON has integer `search_id`; one `SearchQuery` row exists with `corrected_text == "laptop"` and one `Retrieve` row with `product_id == 1`. | Passed |
| BR-INT-026 | Verify that the `POST /api/search` flow returns empty results (no DB fallback) when FAISS reports an empty list. | 1. Patch `text_corrector_service.correct` and `faiss_service.search_text` to return `{"products":[],"success":False}`. <br> 2. POST `/api/search` form `raw_text=zxcvbn`. <br> 3. GET `/api/search/<id>`. | Form `raw_text=zxcvbn`; FAISS returns empty success. | Flask app with `search_bp`. | HTTP 201 on POST; GET returns HTTP 200 with `products: []` and a stored `SearchQuery` having zero `Retrieve` rows. | Failed — *production defect: GET /api/search/<id> uses PostgreSQL-only ARRAY_REMOVE/ARRAY_AGG and 500s on the SQLite test backend* |
| BR-INT-027 | Verify that the `POST /api/search` flow with both text and image dispatches to `search_late_fusion` when `fusion_endpoint=="late"`. | 1. Patch `get_selected_fusion_endpoint` to return `"late"`. <br> 2. Patch `faiss_service.search_late_fusion` to return one product with `text_score`, `image_score`, `combined_score`. <br> 3. POST `/api/search` multipart with `raw_text=red shoe` and an `images` file. | Multipart form with `raw_text` + uploaded image; fusion endpoint = late. | Flask app with `search_bp`; `tmp_path` for the uploaded image. | HTTP 201; `Retrieve` row has `fusion_type == "late_fusion"`, `text_score` and `image_score` populated. | Passed |
| BR-INT-028 | Verify that the `POST /api/search` flow with `search_mode=iwt` calls `faiss_service.search_image_by_text`. | 1. Patch `faiss_service.search_image_by_text` to return one product. <br> 2. POST `/api/search` form `raw_text=blue jacket&search_mode=iwt`. | Form `raw_text=blue jacket&search_mode=iwt`. | Flask app with `search_bp`. | HTTP 201; `search_image_by_text` mock called exactly once with `text="blue jacket"`. | Passed |
| BR-INT-029 | Verify that `POST /api/search` returns HTTP 400 when neither `raw_text` nor an image is provided in `std` mode. | 1. POST `/api/search` form with empty body and `search_mode=std`. <br> 2. Assert HTTP 400. | Empty form, `search_mode=std`. | Flask app with `search_bp`. | HTTP 400; JSON error contains `Either 'raw_text' or 'image' must be provided`. | Passed |
| BR-INT-030 | Verify that `POST /api/search/db-fallback` returns 400 when the body lacks `search_id`. | 1. POST `/api/search/db-fallback` with `{}`. <br> 2. Assert HTTP 400. | `{}`. | Flask app with `search_bp`. | HTTP 400; JSON error `Missing required field: search_id`. | Passed |
